# CML-220 — EVIDENZE_UDA_WORDING_MICROFIX

Data: 2026-06-28

## Scopo

Applicare micro-correzione testuale UI per i due findings noti di CML-218:

1. Label `non_applicabile` mostrava `❌ Esclusa` in `getStateLabel` ma `❌ Non applicabile` nell'evidence panel
2. Status message diceva "proposta non incluse" quando in realtà le evidenze 'proposta' sono incluse

## Modifiche

File: `_published_snapshot/netlify-current/index.html` (3 righe, 3 cambiamenti)

1. **Riga 2706**: `'❌ Esclusa'` → `'❌ Non applicabile'` — label allineata con evidence panel
2. **Riga 2878**: regex match aggiornato da `Esclusa` a `Non applicabile` per conteggio coerenza
3. **Riga 2891**: status message corretto da `"proposta" non vengono incluse` a `"escluse non sono incluse nella bozza UDA"`

## Invarianti preservate

- Stato interno localStorage: `non_applicabile` (invariato) — piena retrocompatibilità
- Comportamento marcatura evidenze: invariato
- Logica generazione bozza UDA: invariata
- Comportamento copia/download Markdown: invariato
- Hash navigation: 14/14 discipline (invariato)
- Schema `.cml`/export/import: invariato
- Curriculum JSON: invariato

## Verifiche

- Validatore: 14/14 PASS
- Shape test: 14/14 PASS
- `git diff --check`: pulito
- `git diff --stat`: solo `index.html`, 3 righe modificate

## Verdetto

`CML_220_EVIDENZE_UDA_WORDING_MICROFIX_READY`
