# CML-115 — SYSTEM_REFOCUS_AFTER_DEPARTMENT_VALIDATION_AUDIT

## Contesto

- Commit: `5ba2322` (CML-114 — VALIDAZIONE_DIPARTIMENTALE_FIRST_RUNTIME_INCREMENT)
- Stato live: 14/0/0
- Tutte le 14 discipline sono in stato "In revisione"
- CML-114 ha aggiunto il pannello informativo di validazione dipartimentale in Curriculum > Consultazione
- CLAUDE.md aggiornato con New Operations Convention e Navigation Map

## Obiettivi

1. Verificare lo stato attuale dopo CML-114.
2. Mappare le sezioni esistenti del runtime.
3. Individuare funzioni già presenti da proteggere.
4. Valutare se Didattica è ancora utile.
5. Proporre la rifocalizzazione in "Competenze e progettazione".
6. Verificare coerenza con la Navigation Map in CLAUDE.md.
7. Proporre il prossimo incremento runtime minimo (CML-116).

## Perimetro

- Audit-only: nessuna modifica runtime.
- Non modificare: index.html, snapshot pubblicato, schema .cml, export/import.
- Non introdurre: persistenza, backend, API, autenticazione, approvazione istituzionale.
- Non rinominare sezioni runtime in questa slice.

## Mappatura Stato Attuale

### Navigazione Implementata (index.html)

- **Home** (`#tab-home`) — Cruscotto, stato revisione, prossima azione, Home cards (Curriculum + Didattica), microguida
- **Curriculum** (`#tab-curricolo`) — Sub-nav: Consulta / Revisione / Definitivo / Fonti
  - Consulta: viewer curricolo di istituto, Tecnologia normalizzata, 14 discipline, pannello validazione dipartimentale (CML-114)
  - Revisione: filtri, import .cml docente, import .cml dipartimentale, export Tecnologia, cards dinamiche
  - Definitivo: riepilogo voci approvate, export Word/Markdown/PDF/.cml/Drive
  - Fonti: sezioni generali e fonti istituzionali
- **Didattica** (`#tab-didattica`) — Sub-nav: Valutazione / Evidenze, UDA modello
  - Valutazione/Evidenze: 13 unità Tecnologia con evidenze osservabili e criteri di valutazione
  - UDA modello: 2 schede UDA
- **Esportazioni** (`#tab-esportazioni`) — Word, Markdown, PDF, backup, report gruppo
- **Guida** (`#tab-guida`) — Come iniziare, Curriculum, Didattica, Esportazioni

### Navigation Map Target (CLAUDE.md)

```
Home
Curriculum
Competenze e progettazione
Validazione dipartimentale
Riepilogo
Esportazioni
Guida
```

### Divergenze Rilevate

1. **Validazione dipartimentale** — non è una sezione autonoma, ma un pannello informativo dentro Curriculum > Consulta (CML-114). Deve rimanere tale o essere spostata?
2. **Didattica** — esiste come area autonoma con Valutazione/Evidenze e UDA modello. Deve diventare "Competenze e progettazione"?
3. **Riepilogo** — esiste come "Definitivo" nella sub-nav di Curriculum. Deve diventare sezione autonoma?
4. **Revisione** — esiste nella sub-nav di Curriculum. Deve rimanere lì o essere integrata altrove?

## Valutazione Didattica

### Cosa contiene

- Evidenze osservabili per Tecnologia (13 unità)
- Criteri di valutazione per Tecnologia
- 2 modelli UDA (Primaria + Secondaria)

### Cosa NON contiene

- Non contiene competenze chiave esplicite
- Non contiene nodi disciplinari
- Non contiene collegamenti interdisciplinari
- Non contiene criteri di continuità verticale
- Non contiene indicazioni operative per i docenti

### Verdetto

- **Mantenere**, ma non come area generica di "Didattica".
- Il contenuto esistente (evidenze, criteri, UDA) è ancora utile come materiale di progettazione.
- Manca il quadro teorico (competenze chiave, nodi disciplinari, continuità verticale).
- Rifocalizzare in **Competenze e progettazione** implica:
  - Rinominare la sezione.
  - Aggiungere contenuti strutturati (non in questa slice audit).
  - Non trasformarla in una seconda guida o archivio generico.

## Proposta di Rifocalizzazione

### Didattica → Competenze e progettazione

- **Nome:** `Competenze e progettazione`
- **Scopo:** raccogliere ciò che non sta bene né in Curriculum né in Validazione:
  - competenze chiave;
  - progettazione per competenze;
  - nodi disciplinari;
  - collegamenti interdisciplinari;
  - criteri di continuità verticale;
  - indicazioni operative per i docenti.
- **Contenuti attuali da preservare:**
  - Evidenze e criteri di valutazione (come materiale di supporto alla progettazione).
  - Modelli UDA (come esempi operativi).

### Non Deve Diventare

- una seconda guida;
- un archivio generico di materiali;
- una sezione ridondante rispetto a Curriculum;
- una raccolta di UDA o programmazioni annuali.

### Coerenza con Navigation Map

La Navigation Map target prevede:

```
Home
Curriculum
Competenze e progettazione
Validazione dipartimentale
Riepilogo
Esportazioni
Guida
```

- **Curriculum** rimane l'area di consultazione/revisione/content.
- **Competenze e progettazione** sostituisce Didattica come area di progettazione curricolare.
- **Validazione dipartimentale** rimane un pannello informativo dentro Curriculum > Consulta (CML-114), non una sezione autonoma. _Nota: la Navigation Map mostra "Validazione dipartimentale" come voce autonoma, ma il contenuto effettivo è già dentro Curriculum. Deve essere chiarito se la Navigation Map intende una sezione separata o se il pannello CML-114 è sufficiente._
- **Riepilogo** (attuale Definitivo) può diventare sezione autonoma nella navigazione principale.
- **Esportazioni** e **Guida** rimangono invariate.

## Prossimo Incremento Runtime Minimo: CML-116

### CML-116 — HOME_CURRICULUM_PROCESS_REFOCUS

Intervento leggero sulla Home per far capire subito il processo:

```
Consulta → Analizza → Proponi → Confronta → Sintetizza → Esporta
```

Azioni:

1. Aggiornare il cruscotto Home con il percorso esplicito.
2. Aggiornare le Home cards per riflettere la nuova mappa (Curriculum + Competenze e progettazione).
3. Aggiornare le breadcrumb e i path guidance in Curriculum > Consultazione e Revisione.
4. Non toccare: validazione, export, import, rollback, service worker.

## Funzioni da Proteggere

- `setTab()` — navigazione principale
- `renderCurricoloIstituto()` — viewer Curriculum (pannello CML-114 incluso)
- `render()` — rendering Revisione
- `renderRiepilogo()` — Definitivo/Riepilogo
- `renderDidattica()` — futuro Competenze e progettazione (attuale Didattica)
- `renderUdaModello()` — futuro Competenze e progettazione
- `selectDisc()` / `resolveDiscFromHash()` — selezione disciplina
- `cardHTML()` — generazione cards revisione
- `approve()` / `reject()` / `undoDecision()` — flusso decisionale
- `saveLocalState()` / `loadLocalState()` — persistenza locale
- Tutte le funzioni di export/import
- Role-access gating

## Validazioni

- `git status` iniziale e finale: invariato (nessuna modifica a file tracciati).
- `git diff --check`: nessun whitespace error.
- Solo documentazione: `docs/03_execution/CML-115.md`, `report/CML-115_*.md`, `docs/REPO-MOVELOG.md`.
- Runtime invariato: `_published_snapshot/netlify-current/index.html` non modificato.
- Nessuna regressione su: Curriculum, Validazione dipartimentale, Revisione, Riepilogo, Esportazioni, Guida.

## Verdetto

```text
CML_115_SYSTEM_REFOCUS_AFTER_DEPARTMENT_VALIDATION_AUDIT_READY
```
