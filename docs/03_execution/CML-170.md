# CML-170 — ARTE_IMMAGINE_RUNTIME_MAPPA_DATI_INTEGRATION

Data: 2026-06-26

## Obiettivo

Integrare Arte e Immagine nella mappa disciplinare runtime del frontend, aggiungendo pulsante selettore, variabile dati e branch switch in `setMappaDisciplina()`, dopo la preparazione del file normalizzato in CML-169.

## Baseline tecnica

- Branch: `main`
- Commit iniziale: `b237cb2`
- origin/main: `b237cb2`
- Working tree iniziale: pulito (sync)
- Test shape runtime: 8/8 PASS
- Validatore curriculum: 9 file, 111 unità, overallValid true, 0 errori
- Push: non eseguito
- Deploy: non eseguito

## Scope autorizzato

- `_published_snapshot/netlify-current/index.html` — tre edit runtime
- `docs/03_execution/CML-170.md`
- `report/CML-170_arte_immagine_runtime_mappa_dati_integration.md`
- `docs/REPO-MOVELOG.md`

## Scope vietato

- `content/curriculum/` — invariato
- `tools/` — invariati
- `_published_snapshot/netlify-current/sw.js` — invariato
- Root `index.html` — invariato
- Schema `.cml` — invariato
- File `.cml` — invariati
- Export/import — invariati
- Funzioni evidenze/UDA — invariate
- `package.json`, `package-lock.json`, `node_modules` — invariati
- `.github` — invariato
- Landing UI/UX 3D — invariata

## Dato normalizzato usato

- `content/curriculum/arte-immagine.normalized.json`

## Intervento runtime effettuato

1. **Pulsante Arte e Immagine**: aggiunto nella barra selettore discipline della sezione mappa disciplinare
2. **Variabile `ARTE_IMMAGINE_MAPPA_DATI`**: variabile JavaScript contenente il dato della mappa (S=6, N=3, P=6, D=0)
3. **Branch `arte-immagine`**: aggiunto `else if` in `renderMappaDisciplinare()` per caricare il dato al click del pulsante

## Conteggi Arte e Immagine

| Proprietà                  | Valore |
| -------------------------- | ------ |
| S (strutture sostanziali)  | 6      |
| N (nodi disciplinari)      | 3      |
| P (progressione verticale) | 6      |
| D (decisioni curricolari)  | 0      |

## Validazioni

| Controllo             | Esito                                                      |
| --------------------- | ---------------------------------------------------------- |
| Validatore curriculum | PASS — 9/9 file, 111 unità, overallValid true, 0 errori    |
| Shape runtime         | PASS — 8/8 invariata (Arte e Immagine non ancora nel test) |
| `git diff --check`    | PASS — nessun whitespace error                             |
| `git diff --stat`     | 4 insertions, 0 deletions — solo `index.html`              |

## Conferme invarianti

- `content/curriculum/` — invariato (nessuna modifica ai file normalizzati)
- `tools/` — invariati (test shape non aggiornato in questa slice)
- Schema `.cml` — invariato
- Export/import — invariati
- Funzioni evidenze/UDA — invariate
- Nessuna dipendenza aggiunta

## Conferma test shape

Il test shape runtime (`tools/test-runtime-mappa-dati-shape.mjs`) NON è stato aggiornato in questa slice. Rimane a 8/8 discipline. L'aggiornamento a 9/9 è rimandato a CML-171.

## Conferma nessun deploy

Deploy non eseguito.

## Conferma nessun push

Push non eseguito.

## Verdict locale

```
CML_170_ARTE_IMMAGINE_RUNTIME_MAPPA_DATI_READY
```
