# CML-367 — Teacher Review Always-Visible Comparison and SVG Functional Icons

## Mandatory Slice Header
- Macroprogramma: PM-07 Uniformità
- Backlog: UX-025 (icone funzionali non coerenti, confronto nascosto)
- Dipende da: CML-366 (baseline pubblicata)
- Tipo slice: runtime increment

## Azioni
1. CSS: confronto 2012/2025 sempre visibile (`.pending-detail{display:block}`)
2. Rimossa funzione `togglePendingDetail(id)` e pulsante dettaglio con `pdb-`
3. Rimossi riferimenti morti `detailStartsOpen`, `detailLabel`, `id="pd-..."`
4. Aggiunte icone SVG `icon-undo` e `icon-save` nello sprite
5. Sostituiti emoji funzionali con SVG nei pulsanti `.act` della Revisione docente:
   - `✏ Modifica` → `icon-edit` + testo
   - `🗑 Rimuovi` → `icon-trash` + testo
   - `↩ Cambia idea` → `icon-undo` + testo
   - `✅ Salva e Approva` → `icon-check` + testo
6. Modifiche equivalenti su `index.html` e `_published_snapshot/netlify-current/index.html`

## Bug preesistenti (non corretti in questa slice)
- `icon-upload` e `icon-landmark`: verifica ispettiva ha confermato che ENTRAMBE hanno definizione `<symbol>` completa nello sprite — nessun bug riscontrato. Segnalazione originaria non confermata dal codice corrente.

## File modificati
- `index.html`
- `_published_snapshot/netlify-current/index.html`

## Funzioni/aree toccate
- CSS: `.pending-detail` (sempre visibile)
- SVG sprite: `icon-undo`, `icon-save` (nuovi symbol)
- `cardHTML()`: rimozione toggle, sostituzione emoji con SVG

## Controlli eseguiti
- `git diff --check`: PASS
- assenza `togglePendingDetail`: OK
- assenza `pdb-`: OK
- presenza nuovi symbol: `icon-undo` + `icon-save`: OK
- badge decorativi non toccati: OK
- emoji funzionali sostituiti: OK
- `validate-cml-normalized-curriculum.mjs`: 14/14 PASS
- `test-runtime-mappa-dati-shape.mjs`: 14/14 PASS
- sintassi script inline (`new Function()`): 2/2 PASS (entrambi i file)

## Esclusioni intenzionali
- Badge decorativi (`★ Nuova`, `📍`, `🆕`, `📄`, `✅ Approvata`, `❌ Rifiutata`)
- Etichette pannello (`✏ DM 221/2025...`)
- Toast/notifiche con emoji
- CSS `.pending-action.detail` (dead code, harmless)
- Icone `icon-upload`/`icon-landmark` (già definite, nessun bug)

## Verdict
`CML_367_TEACHER_REVIEW_ALWAYS_VISIBLE_COMPARISON_AND_SVG_FUNCTIONAL_ICONS_READY_LOCAL_NOT_PUSHED`
