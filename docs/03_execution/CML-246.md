# CML-246 — PROCESS FLOW CLARITY MICRO-UX

Tipo slice: runtime micro-UX + documentazione
Runtime modificato: si (in modo limitato)
Deploy: non eseguito
Push: non eseguito
Verdict atteso: `CML_246_PROCESS_FLOW_CLARITY_MICRO_UX_READY_LOCAL_NOT_PUSHED`

## Contesto post CML-245/CML-245P

Dopo la riallocazione priorita UX in CML-245 e il push controllato CML-245P, questa slice introduce un miglioramento micro-UX nel runtime per rendere esplicito il percorso operativo tra ruoli scolastici.

## Obiettivo UX

Rendere immediatamente leggibile il flusso:

1. Docente -> prepara proposta
2. Dipartimento -> confronta e registra esito
3. Referente -> controlla e valida
4. Documento finale -> esporta/archivia

Principio guida applicato: "una schermata, una decisione".

## Descrizione intervento runtime

- Inserito in Home un blocco compatto `home-microguide` con titolo "Percorso di lavoro", sottotitolo e 4 fasi operative.
- Ogni fase include ruolo, azione principale, esito atteso.
- Inserito microtesto "Dove mi trovo?" in chiusura del blocco, senza introdurre nuove azioni.
- Aggiornato il testo introduttivo della sezione Processo (`esport-intro-tip`) per allinearlo alla stessa sequenza a 4 fasi.
- Nessuna modifica a logica JS, tab/hash/sidebar, storage, import/export, schema `.cml` o contratti dati.

## File modificati

- `index.html`
- `_published_snapshot/netlify-current/index.html`
- `docs/03_execution/CML-246.md`
- `report/CML-246_process_flow_clarity_micro_ux.md`
- `docs/REPO-MOVELOG.md`

## Sezioni interessate

- Home (`#tab-home`)
- Processo (`#tab-processo`)
- Esportazioni: mantenuta come destinazione esplicita nel percorso, senza modifiche funzionali

## Invarianti rispettate

- Nessuna modifica a `README.md`, `content/`, `tools/`, JSON, service worker, manifest, asset, deploy config
- Nessuna nuova dipendenza
- Nessuna integrazione rete/API/OAuth/backend
- Nessuna modifica schema `.cml`
- Nessuna modifica chiavi localStorage/sessionStorage
- Nessuna modifica contratti import/export dati

## Controlli eseguiti

- Preflight Git: branch `main`, allineato a `origin/main`, working tree pulito, nessun ahead iniziale
- `git diff --check`: pulito
- Validator: `node tools/validate-cml-normalized-curriculum.mjs` eseguito (esito valido con warning retrocompatibili noti)
- Shape test: `node tools/test-runtime-mappa-dati-shape.mjs` eseguito (PASS)
- Smoke runtime: Home/Processo/Esportazioni raggiungibili; hash/tab invariati; componente visibile e leggibile
- Secret scan minimale sui file della slice: pulito

## Esito smoke

- Home caricata correttamente
- Navigazione verso Processo funzionante
- Navigazione verso Esportazioni funzionante
- Selezione disciplina e struttura tab invariata
- Nessun errore sintattico introdotto nel runtime

## Stato operativo

- Push non eseguito
- Deploy non eseguito

## Verdict

`CML_246_PROCESS_FLOW_CLARITY_MICRO_UX_READY_LOCAL_NOT_PUSHED`
