# CML-475 — Third Gap Pilot Source Gate Verification

## Obiettivo

Verificare se il candidato selezionato da CML-474 — Musica, unità `mus_pri_3_001`, campo `criteriValutazione` — dispone di un delta testuale esatto, documentato e non ridondante sufficiente a creare un terzo pacchetto Gap canonico.

La slice è esclusivamente di audit e decisione. Non introduce pacchetti Gap, modifiche runtime o modifiche curricolari.

## Baseline

- `main`: `589bc569c739b1094d3790a9cbe1a4ea84ba134d`
- CML-474 integrata: `criteriValutazione` selezionato come prossimo campo B03
- candidato condizionato: Musica, `mus_pri_3_001`
- gate richiesto: delta esatto, fonte verificabile, differenza semantica genuina, validazione umana obbligatoria

## Testo vigente

Nel JSON normalizzato corrente, `mus_pri_3_001.criteriValutazione` contiene:

1. `Correttezza nella lettura ritmica e melodica`
2. `Precisione ritmica nell'esecuzione`
3. `Intonazione e partecipazione nel canto corale`
4. `Capacità di scrittura musicale di base`

Questo costituisce il solo `testoOriginale` ammissibile per un eventuale pacchetto Gap sul campo `criteriValutazione`.

## Fonti interne verificate

Sono stati controllati:

- `report/CML-173_musica_readiness_data_audit.md`
- `docs/03_execution/CML-173.md`
- `content/curriculum/musica.normalized.json`
- documenti storici richiamati da CML-173: CML-104, CML-107, CML-109, CML-165 e CML-172
- contratto Gap `gap-layer-v1`
- contratto decisionale B03 e CML-474

## Esito del source gate

Il gate non è soddisfatto.

### Evidenze negative determinanti

1. CML-173 dichiara che non esistono proposte IN 2025 strutturate come campo dedicato per Musica.
2. CML-173 prescrive `propostaIN2025: null` per tutte le unità.
3. CML-173 dichiara che non esistono decisioni curricolari documentate per Musica.
4. I riferimenti a notazione musicale, canto corale, musica d'insieme e tecnologie digitali descrivono aree di readiness, non un testo proposto sostitutivo per `criteriValutazione`.
5. Nessun audit esistente fornisce una coppia verificabile `testoOriginale` / `proposto` per `mus_pri_3_001.criteriValutazione`.
6. Derivare nuovi criteri dalla descrizione generale della disciplina produrrebbe contenuto sintetico non autorizzato.

## Decisione

Non viene creato alcun file:

- `content/gap/musica.gap.json`
- `curman-react/src/data/gap/musica.gap.json`

Non vengono modificati:

- schema Gap;
- validator;
- runtime React;
- curricoli normalizzati;
- audit Playwright.

## Implicazione per B03

La copertura resta correttamente attestata sui due pilot verificati:

| Pilot | Disciplina | Unità | Campo | Stato |
|---|---|---|---|---|
| 1 | Educazione Fisica | `ef_sec_3_001` | `obiettivi` | verificato e integrato |
| 2 | Tecnologia | `tec_pri_1_001` | `traguardo` | verificato e integrato |

Il terzo pilot resta sospeso finché non emergerà da una fonte o da una validazione dipartimentale un delta esatto e tracciabile.

## Prossimo passo ammesso

Non avviare CML-476 su tre pilot.

La prossima slice utile deve essere una delle seguenti:

1. audit trasversale degli archivi esistenti per trovare una disciplina con coppia testuale esatta già documentata;
2. acquisizione e validazione umana di una proposta disciplinare reale;
3. consolidamento B03 sui due pilot correnti senza espansione artificiale della copertura.

## Vincoli rispettati

- nessun contenuto disciplinare inventato;
- nessun pacchetto Gap sintetico;
- nessuna modifica runtime;
- nessuna modifica curricolare;
- nessun backend, autenticazione o telemetria;
- nessun deploy.

## Verdetto

```text
CML_475_NO_VERIFIED_THIRD_PILOT_AVAILABLE_SOURCE_GATE_FAILED_NO_GAP_PACKAGE_CREATED
```
