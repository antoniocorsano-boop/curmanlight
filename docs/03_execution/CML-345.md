# CML-345 — Dashboard Export and Navigation Coherence Microfix

## 1. Stato iniziale
- Branch: `main`
- Baseline commit: `598441a` (`docs: finalize CML-344 pushed and published closure`)
- Stato rispetto a origin/main: allineato
- Working tree iniziale: pulito
- Slice precedente: CML-344 (chiusa e pubblicata)

## 2. Obiettivo
Correggere incoerenze operative residue su dashboard e navigazione senza toccare dati curricolari, schema `.cml`, import/export model, validatori, contenuti disciplinari o logiche di ruolo.

## 3. Perimetro applicato
- Runtime pair:
  - `index.html`
  - `_published_snapshot/netlify-current/index.html`
- Documentazione slice:
  - `docs/03_execution/CML-345.md`
  - `report/CML-345_dashboard_export_and_navigation_coherence_microfix.md`
  - `docs/REPO-MOVELOG.md`
  - aggiornamento minimo stato/progresso di governance

## 4. Microfix runtime applicate
1. CTA cruscotto `Esporta`:
   - da `setTab('lavoro')` con scroll toolbar
   - a `setTab('esportazioni')`
2. Lessico Curriculum/Curricolo:
   - etichetta principale tabbar desktop allineata a `Curricolo`
   - breadcrumb/label runtime allineati a `Curricolo`
   - voce mobile menu allineata a `Curricolo`
3. Ammorbidimento `Definitivo`:
   - sub-nav curriculum: `Finale in verifica`
   - titolo vista riepilogo: `Curricolo finale in verifica`
   - breadcrumb/label runtime: `Curricolo — Finale in verifica`
4. Microcopy salvataggio locale:
   - aggiunta nel blocco `File e salvataggio` (quick actions) per distinguere salvataggio browser da sezione `Esportazioni`.

## 5. Controlli eseguiti
- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS (`overallValid: true`, `invalidCount: 0`)
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS (`overall: PASS`, `14 passed, 0 failed`)
- Parita root/snapshot sui marker runtime CML-345: PASS

### Smoke Playwright (locale)
1. Cruscotto `Esporta` apre Esportazioni: PASS
2. Cruscotto `Controlla voci` apre Revisione con filtro pending: PASS
3. Cruscotto `Apri documento` apre riepilogo: PASS
4. Tabbar desktop mostra `Curricolo` (non `Curriculum`): PASS
5. Sub-nav mostra `Finale in verifica` (non `Definitivo`): PASS
6. Riepilogo mostra `Curricolo finale in verifica`: PASS
7. Esportazioni mantiene gruppi e pulsanti: PASS
8. Guida coerente con scaricamenti centralizzati in Esportazioni: PASS

## 6. Invarianti confermate
- Nessuna modifica a schema `.cml`
- Nessuna modifica dati disciplinari
- Nessuna modifica validatori
- Nessuna modifica import/export model
- Nessuna modifica logiche di ruolo

## 7. Esito
- Slice pronta al commit locale.
- Push: non eseguito.
- Deploy: non eseguito.

## 8. Verdict
`CML_345_DASHBOARD_EXPORT_AND_NAVIGATION_COHERENCE_MICROFIX_READY_LOCAL_NOT_PUSHED`
