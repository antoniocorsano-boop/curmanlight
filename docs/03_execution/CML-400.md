# CML-400 — User Validation Entry Runtime

- Macroprogramma: PM-09 Validazione con utenti
- Dipende da: CML-399 (User Validation Scenario Pack), CML-398 (Protocollo di test utente leggero)
- Tipo slice: runtime micro-patch
- Runtime: modificato (index.html, _published_snapshot/netlify-current/index.html)
- Main: non ancora pushato

## Obiettivo

Inserire nella Home un accesso provvisorio al test guidato per docenti, coerente con CML-399.

## Cambiamenti

### Runtime (per file)

1. **CSS** — Aggiunte classi `.home-validation-entry` (card Home), `.validation-overlay`, `.validation-dialog`, `.validation-intro`, `.validation-scenario`, `.validation-tasks`, `.validation-prudent`, `.validation-actions`, `.validation-primary`, `.validation-secondary`
2. **Home HTML** — Inserita section `.home-validation-entry` dopo le card "Operazioni di supporto", prima della context card. Titolo "Partecipa al test guidato", descrizione, pulsante "Avvia test guidato"
3. **JS** — Aggiunte `openValidationPanel()`, `closeValidationPanel()`, `exportValidationObservations()`

### Non modificati

- Dati curricolari, flusso .cml, navigazione, export, backend, deploy, service-worker.js
- Logiche di validazione, approvazione, revisione, progettazione

## Validazione

- `git diff --check`: ok
- Curriculum validator: 14/14 PASS
- Runtime shape test: 14/14 PASS
- Runtime pair: allineato (stesse marche identiche)

## Verdetto

CML_400_USER_VALIDATION_ENTRY_RUNTIME_READY_LOCAL_NOT_PUSHED
