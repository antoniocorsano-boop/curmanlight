# CML-434 — Home Task Selector Runtime

- Macroprogramma: PM-09 Validazione con utenti
- Dipende da: CML-434D (design/mock alternatives), CML-434S (runtime remote safety gate)
- Tipo slice: runtime micro-slice (su branch)
- Runtime: modificato (index.html, _published_snapshot/netlify-current/index.html)
- Branch: codex/cml-434-home-task-selector-runtime (pushata su origin, PR #24 aperta, non mergiata)

## Obiettivo

Sostituire i pulsanti operativi della Home con un selettore di 4 attività docente (Consulta, Progetta, Propone, Esporta) e un pannello contestuale informativo, senza ricostruire la Home da zero.

## Cambiamenti

### Runtime (per file)

1. **CSS** — Rimosse classi morte `.home-process-hub__actions`, `.home-process-hub__btn`, `.home-process-hub__btn.primary`; aggiunte classi `.home-task-selector`, `.home-task-btn`, `.home-task-btn--active`, `.home-task-context`, `.home-task-context-title`, `.home-task-context-body`, `.home-task-context-list`, `.home-task-context-actions`, `.home-task-context-btn`; aggiornata media query mobile
2. **Home HTML** — Sostituito `.home-process-hub__actions` con `.home-task-selector` contenente 4 bottoni; aggiunto `.home-task-context` dopo il selettore; conservati header hub, pipeline, aree, governance strip
3. **JS** — Aggiunte `HOME_TASK_CONTEXTS` (4 contesti), `setHomeTaskContext(task, opts)`, `renderHomeTaskContext()`; chiamata `renderHomeTaskContext()` in `setTab('home')` e init; bottone "Proponi un aggiornamento" naviga a `setTab('lavoro')`

### Non modificati

- Dati curricolari, flusso .cml, navigazione, export, backend, deploy, service-worker.js
- Blocchi hub esistenti (header, pipeline, aree, governance strip)
- Logiche di validazione, approvazione, revisione, progettazione

## Validazione

- `git diff --check`: ok
- Curriculum validator: 14/14 PASS
- Runtime shape test: 14/14 PASS
- Runtime pair: allineato (28 occorrenze `home-task` in entrambi i file)

## Verdetto

CML_434_HOME_TASK_SELECTOR_RUNTIME_PUSHED_BRANCH_NOT_MERGED
