# CML-261 — ROLE LANGUAGE SURFACE CLEANUP

## Contesto post CML-260

CML-260 ha prodotto un audit multi-agente (68 findings) con una roadmap di 6 slice, di cui CML-261 era la prima: runtime microfix per la superficie linguistica.

CML-260 commit (`84375e1`) è docs-only e ancora locale. CML-261 parte dal working tree sporco su `index.html` e `_published_snapshot/netlify-current/index.html`.

## Stato repository

- Branch: `main`
- HEAD: `84375e1` (CML-260 — docs-only, locale, non pushato)
- origin/main: `fb306dd` (CML-259P)
- Ahead: 1 (CML-260)
- Working tree: modificato (CML-261 runtime change)
- `git diff --check`: pulito

## Obiettivo

Applicare le seguenti sostituzioni linguistiche su `index.html` e snapshot, come da style dictionary CML-260 Agente 5:

| Vecchio | Nuovo | Occorrenze |
|---------|-------|------------|
| `UDA smart` (testo UI) | `UDA personalizzata` | 6+ nel testo UI statico |
| `persistita` / `persistito` | `salvata` / `salvato` | 2 |
| `Reset` (come testo pulsante) | `Ripristina` | 1 (pulsante marcature) |
| `Piu` (typo) | `Più` | 1 |

**Non modificate:** CSS class names (`.evidence-reset-btn`, `resetEvidenceStates()`) e nomi funzione JS — sono nomi tecnici interni, non superficie UI.

## Modifiche applicate

**File modificati:**
- `index.html` — 26 linee (13 insertion, 13 deletion)
- `_published_snapshot/netlify-current/index.html` — 26 linee (13 insertion, 13 deletion)

**Dettaglio sostituzioni:**

1. **"UDA smart" → "UDA personalizzata"** (19 occorrenze totali tra testo statico, placeholder, fallback)
   - Badge e label nella vista Didattica/Programmazione
   - Testo esplicativo e placeholder input
   - Testo nei messaggi di stato vuoto
   - Etichette in libreria, anteprime e markdown
   - Livello 4 nella legenda livelli maturità
   - Note docente e privacy notice
   - Nome bozza di default

2. **"persistita" → "salvata"** (1 occorrenza)
   - `La bozza non Ã¨ salvata`

3. **"persistito" → "salvato"** (1 occorrenza)
   - `Il lavoro resta salvato in locale`

4. **"Reset" (pulsante) → "Ripristina marcature"** (1 occorrenza)
   - Pulsante `.evidence-reset-btn` nel pannello Evidenze

5. **"Piu" → "Più"** (1 occorrenza)
   - `Più recenti` nella select ordinamento UDA

## Verifiche

| Controllo | Esito |
|-----------|-------|
| `git diff --check` | Pulito |
| Validatore curriculum (14 file) | 14/14 valid, overallValid true, invalidCount 0 |
| Shape test mappa dati | 14/14 PASS |
| Smoke statico — vecchi termini residui | Nessun `UDA smart`, `persistita`, `Reset` (come testo UI), `Piu` |
| Smoke statico — nuovi termini presenti | `UDA personalizzata`, `salvata`, `Ripristina marcature`, `Più recenti` |

**Note validatore:** warning preesistenti (non introdotti da CML-261) confermati.

## Runtime invariato

Nessuna modifica a logica JS, storage, esportazioni, schema `.cml`, curriculum JSON, service worker, manifest, tools.

## Deploy non eseguito

Nessun deploy manuale.

## Verdict

```
CML_261_ROLE_LANGUAGE_SURFACE_CLEANUP_READY_LOCAL_NOT_PUSHED
```
