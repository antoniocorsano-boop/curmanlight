# CML-163 — INGLESE_CONTROLLED_RUNTIME_MAPPA_DATI_INTEGRATION

Data: 2026-06-26

## Stato iniziale

- Root Git: `C:/Users/anton/CurManLight`
- Branch: `main`
- Commit iniziale: `0dd411d`
- `origin/main`: `0dd411d`
- Working tree iniziale: pulito (sync con origin/main)
- Push: non eseguito
- Deploy: non eseguito

## Baseline tecnica

Runtime mappa pre-esistente: Tecnologia, Matematica, Italiano, Scienze, Storia, Geografia

## Perimetro

File autorizzati:

- `_published_snapshot/netlify-current/index.html`
- `docs/03_execution/CML-163.md`
- `report/CML-163_inglese_runtime_mappa_dati_integration.md`
- `docs/REPO-MOVELOG.md`

Non sono state modificate: schema `.cml`, export/import, service worker, validatori, dipendenze, root `index.html`, landing UI/UX, contenuti normalizzati JSON, tools, evidenze/UDA, funzioni generatrici.

## Modifiche eseguite

In `_published_snapshot/netlify-current/index.html`:

- aggiunto il pulsante `Inglese` nel selettore mappa disciplinare (dopo Geografia);
- aggiunta la variabile runtime `INGLESE_MAPPA_DATI`;
- aggiunto il ramo `inglese` in `setMappaDisciplina()`.

La struttura `INGLESE_MAPPA_DATI` contiene:

- 12 strutture sostanziali;
- 11 nodi disciplinari;
- 9 progressioni verticali;
- 0 decisioni curricolari.

`D=0` è accettato: la shape runtime passa (PASS, 7/7) e non sono state inventate decisioni curricolari fittizie. Inglese non ha ancora un audit preparatorio.

## File modificati

- `_published_snapshot/netlify-current/index.html`

## Validazioni eseguite

- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS, 7 file / 94 unità / `overallValid: true` / 0 errori
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS, 7 discipline / 0 failed (Inglese: S=12 N=11 P=9 D=0)
- verifica codifica blocco `INGLESE_MAPPA_DATI`: PASS, JSON parseable, nessun `\uFFFD`, nessun `???`, nessun `??`, accenti italiani conservati

## Conferme invarianti

- Tecnologia: invariata
- Matematica: invariata
- Italiano: invariato
- Scienze: invariato
- Storia: invariata
- Geografia: invariata
- Evidenze/UDA: invariate
- Schema .cml: non modificato
- Export/import: non modificato
- Service worker: non modificato
- Dipendenze: non modificate
- Landing UI/UX: non modificata

## Esito

La slice integra Inglese nel runtime locale senza estendere altre discipline e senza modifiche collaterali fuori perimetro.

Nessun deploy eseguito.
Nessun push eseguito.

Verdetto:

`CML_163_INGLESE_CONTROLLED_RUNTIME_MAPPA_DATI_READY`
