# CML-263 — VISIBLE GOVERNANCE REFERENCES CLEANUP

Tipo slice: runtime microfix + docs
Runtime modificato: si (microcopy-only)
Deploy: non eseguito
Push: non eseguito
Verdict atteso: `CML_263_VISIBLE_GOVERNANCE_REFERENCES_CLEANUP_READY_LOCAL_NOT_PUSHED`

## Contesto post CML-262R/CML-262RP

La revisione umana post CML-262/CML-262R ha confermato un gap residuo nella UI pubblica: presenza di riferimenti interni di governance (codici CML) visibili all'utente finale.

## Evidenza del problema

Testi visibili in UI non operativi per i ruoli scolastici:

- "Anteprima consultiva basata sul contratto dati CML-061."
- "Fonte: CML-061 / CML-062"

## Obiettivo

Rimuovere dalla UI pubblica i riferimenti visibili di governance e sostituirli con formule operative comprensibili, mantenendo invariata la logica del runtime.

## Perimetro runtime microcopy-only

File runtime modificati:

- `index.html`
- `_published_snapshot/netlify-current/index.html`

Nessuna modifica a funzioni JavaScript, schema `.cml`, storage, import/export dati, classi/ID/hook runtime.

## Testi rimossi/sostituiti

1. Da:
   - "Anteprima consultiva basata sul contratto dati CML-061. Contenuti da validare in sede dipartimentale."
   A:
   - "Anteprima consultiva da verificare in sede di lavoro disciplinare."

2. Da:
   - "Stato: da validare · Fonte: CML-061 / CML-062"
   A:
   - "Stato: da validare · Base di lavoro: materiali curricolari già predisposti"

## Motivazione delle nuove formule

- Rimozione codici interni non utili all'utente scolastico
- Riduzione carico cognitivo su docente/dipartimento/referente
- Conservazione del significato operativo del messaggio

## Controlli eseguiti

- Preflight reale con working tree non pulito ammesso: PASS
- Diff runtime (`diff` + `word-diff`): PASS (microcopy-only)
- Ricerca residui governance in runtime: PASS
- Verifica nuove formule operative: PASS
- Root/snapshot consistency (`numstat`, `stat`, size): PASS
- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS
- Smoke locale minimo: PASS

## Esito validatore

PASS (14/14 valid, warning retrocompatibili già noti).

## Esito shape test

PASS (14/14).

## Esito ricerca residui

- Zero occorrenze runtime sui pattern target di governance visibile (`contratto dati CML-061`, `Fonte: CML-061 / CML-062`).
- Occorrenze CML restano in documentazione tecnica e dati interni, non in testo UI pubblico oggetto di questa slice.

## Esito root/snapshot consistency

- Diff bilanciato su entrambe le copie runtime
- Dimensione file root/snapshot allineata

## Invarianti

- Schema `.cml` invariato
- Storage invariato
- Import/export dati invariati
- Deploy non eseguito
- Push non eseguito

## Prossima slice consigliata

CML-263P (controlled push) seguita da smoke live dedicato, se previsto dal flusso operativo.

## Verdict

`CML_263_VISIBLE_GOVERNANCE_REFERENCES_CLEANUP_READY_LOCAL_NOT_PUSHED`
