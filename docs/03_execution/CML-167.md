# CML-167 — EDUCAZIONE_CIVICA_RUNTIME_MAPPA_DATI_INTEGRATION

Data: 2026-06-26

## Obiettivo

Integrare Educazione Civica nel runtime CurManLight, portando la copertura mappa runtime da 7/14 a 8/14 discipline, senza modificare architettura, schema, tool o altre discipline.

## Baseline tecnica

- Branch: `main`
- Commit iniziale: `967ab28` (CML-166 sync closed remote)
- origin/main: `967ab28`
- Working tree iniziale: pulito
- Push: non eseguito
- Deploy: non eseguito

## Scope autorizzato

- `_published_snapshot/netlify-current/index.html`
- `docs/03_execution/CML-167.md`
- `report/CML-167_educazione_civica_runtime_mappa_dati_integration.md`
- `docs/REPO-MOVELOG.md`

## Scope vietato

- root `index.html` — invariato (confermato)
- `content/curriculum/` — invariato
- `tools/` — invariati
- schema `.cml` — invariato
- export/import — invariati
- funzioni evidenze/UDA — invariate
- service worker — invariato
- dipendenze — nessuna introdotta

## Dati normalizzati usati

- `content/curriculum/educazione-civica.normalized.json` (CML-166) — 11 unità, 4 nuclei, 3 ordini — letto per generare la mappa dati runtime, non modificato

## Intervento runtime effettuato

Tre modifiche puntuali in `_published_snapshot/netlify-current/index.html`:

### 1. Pulsante selettore

Aggiunto dopo il pulsante Inglese:

```html
<button
  class="mappa-disc-btn"
  onclick="setMappaDisciplina('educazione-civica',this)"
  ...
>
  Educazione Civica
</button>
```

### 2. Variabile mappa dati

Inserita dopo `INGLESE_MAPPA_DATI`:

```js
var EDUCAZIONE_CIVICA_MAPPA_DATI = { ... };
```

### 3. Branch di selezione

Aggiunto in `renderMappaDisciplinare()` dopo il branch Inglese:

```js
else if (mappaDisciplinaCorrente === 'educazione-civica' && typeof EDUCAZIONE_CIVICA_MAPPA_DATI !== 'undefined') dati = EDUCAZIONE_CIVICA_MAPPA_DATI;
```

## Conteggi Educazione Civica

| Campo                 | Valore |
| --------------------- | :----: |
| struttureSostanziali  |   11   |
| nodiDisciplinari      |   4    |
| progressioneVerticale |   9    |
| decisioniCurricolari  |   0    |

## Validazioni eseguite

| Controllo                                      |                          Esito                           |
| ---------------------------------------------- | :------------------------------------------------------: |
| validate-cml-normalized-curriculum.mjs         |    ✅ 8 file, 105 unità, overallValid true, 0 errori     |
| test-runtime-mappa-dati-shape.mjs              |          ✅ 7/7 PASS (7 discipline pre-CML-166)          |
| Verifica codifica EDUCAZIONE_CIVICA_MAPPA_DATI |        ✅ JSON valido, no \\uFFFD, no ???, no ??         |
| git diff --check                               |               ✅ nessun errore whitespace                |
| Soli file autorizzati modificati               | ✅ solo `_published_snapshot/netlify-current/index.html` |

## Conferme invarianti

- `content/curriculum/` — nessuna modifica ✅
- `tools/` — nessuna modifica ✅
- schema `.cml` — nessuna modifica ✅
- export/import — nessuna modifica ✅
- funzioni evidenze/UDA — nessuna modifica ✅
- service worker — nessuna modifica ✅
- dipendenze — nessuna introdotta ✅
- Deploy — non eseguito ✅
- Push — non eseguito ✅

## Verdict

`CML_167_EDUCAZIONE_CIVICA_RUNTIME_MAPPA_DATI_READY`
