# CML-375 — Runtime Access Refactor Chain Pre-Push Audit

## Mandatory Slice Header
- Macroprogramma: (autonomo — pre-push audit della catena CML-371→CML-374)
- Dipende da: CML-374 (residual hardcode audit)
- Tipo slice: docs-only
- Perimetro: certificazione stato catena prima del push

## Obiettivo
Certificare formalmente che l'intera catena CML-371 → CML-374 è pronta per il push, convalidando working tree pulito, zero residui hardcoded, e test superati.

## Audit

### Working tree
```bash
git status --short
```
**Risultato:** pulito ✅ (solo `CurManLightBrain/` non tracciato)

### Commit log
```bash
git log --oneline -4
```
```
fa8d084 docs: curriculum runtime access residual hardcode audit (CML-374)
43a932d runtime: remove Tecnologia normalized dead code (CML-373)
87f1fc0 runtime: introduce getUnitsForDiscipline and remove tecnologia guards (CML-372)
cddccec runtime: introduce MAPPA_DATI_INDEX access helper (CML-371)
```
**Risultato:** catena lineare di 4 commit ✅

### Whitespace check
```bash
git diff --check
```
**Risultato:** nessun errore ✅

### Residui hardcoded

| Pattern | Hits | Esito |
|---------|:----:|:-----:|
| `TECNOLOGIA_NORM` / `TECNOLOGIA_NORM_DATA` | 0 | ✅ |
| `*_NORM` | 0 | ✅ |
| `ALL_CURRICULUM_DATA.<disciplina>` (dot-access) | 0 | ✅ |
| `discKey === 'tecnologia'` | 0 | ✅ |

### Validazioni

| Test | Risultato |
|------|-----------|
| `validate-cml-normalized-curriculum.mjs` | overallValid: true, 14 file, 142 unità ✅ |
| `test-runtime-mappa-dati-shape.mjs` | overall: PASS, 14/14 ✅ |

## Riepilogo catena CML-371 → CML-374

| Slice | Commit | Cosa fa |
|-------|--------|---------|
| CML-371 | `cddccec` | Helper `MAPPA_DATI_INDEX` + `getMappaForDiscipline()` |
| CML-372 | `87f1fc0` | `getUnitsForDiscipline(discKey)` + rimozione 4 guardie Tecnologia |
| CML-373 | `43a932d` | Rimozione `TECNOLOGIA_NORM` / `TECNOLOGIA_NORM_DATA` dead code |
| CML-374 | `fa8d084` | Audit residui: zero hit certificati |

## Verdict
```text
CML_375_RUNTIME_ACCESS_REFACTOR_CHAIN_PRE_PUSH_AUDIT_READY_LOCAL_NOT_PUSHED
```

La catena è pronta per push controllato o merge/PR verso `main`.
