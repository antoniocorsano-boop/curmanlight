# CML-218 — EVIDENZE_UDA_WORKFLOW_SMOKE_AND_CLOSURE_GATE

Data: 2026-06-28

## Scopo

Eseguire smoke end-to-end e closure gate per il ciclo Evidenze/UDA workflow (CML-214, CML-215, CML-216, CML-217), verificando staticamente e in pubblico (GitHub Pages) che il ciclo sia completo e funzionante, e documentarne i limiti noti.

## Pre-flight checks

- `git status --short --branch`: working tree pulito, HEAD = origin/main = `5a5f65f`
- `node tools/validate-cml-normalized-curriculum.mjs`: 14/14 PASS
- `node tools/test-runtime-mappa-dati-shape.mjs`: 14/14 PASS
- `git diff --check`: pulito
- Secret scan: nessun secret
- GitHub Pages: HTTP 200, app caricata

## Verifiche eseguite

### A. Static code analysis (5 punti)

1. **localStorage key**: solo `cml_evidenze_state` per stato evidenze; nessuna chiave UDA draft, nessun dato studente/gradi — ✅
2. **Markdown UDA fields**: tutte e 6 le sezioni presenti (Riferimenti curricolari, Evidenze selezionate, Criteri, Attività, Adattamenti, Nota privacy) — ✅
3. **Privacy notice**: presente nelle righe 2849-2851 della bozza Markdown — ✅
4. **Evidenza esclusa**: la UI usa solo stato `non_applicabile` per "Escludi" (linea 2564). `buildUdaDraftMarkdown` alla riga 2808 lo esclude correttamente (`state !== 'non_applicabile'`). Tuttavia la label in `getStateLabel` (linea 2706) mappa `non_applicabile` → `❌ Esclusa` mentre nell'evidence panel (linea 2563) è mappato come `❌ Non applicabile` — **inconsistenza di label minore**. Inoltre lo status message alla riga 2891 dice "Le evidenze in stato 'proposta' non vengono incluse" ma in realtà le evidenze 'proposta' VENGONO incluse (riga 2808 include tutto ciò che non è `non_applicabile`) — **bug minore UI text**.
   - Giudizio complessivo: ⚠️ logica corretta, label e messaggio da allineare
5. **Funzioni `.cml` workflow**: tutte invariate rispetto a CML-217 (export/import proposal/outcome, validation, rendering) — ✅

### B. Public GitHub Pages smoke

- URL: https://antoniocorsano-boop.github.io/curmanlight/
- HTTP 200, app caricata
- Evidence panel: visibile, stati Adottata/Adattata/Escludi funzionanti
- UDA draft panel: selettori disciplina/unità, generazione bozza, copia/download Markdown funzionanti
- Hash navigation: 14/14 discipline raggiungibili via hash

### C. Representative discipline workflow (5 discipline)

- Tecnologia, Italiano, Matematica, Scienze, Storia: tutte con evidenze marcabili, UDA draft generabile

### D. localStorage isolation

- `cml_evidenze_state`: usato solo per evidenze
- `cml_local_save`: usato per salvataggio locale (invariato)
- Nessuna altra key UDA-related

## Limitazioni documentate

- UDA draft non è persistito (solo bozza in memoria, copia/download manuale)
- UDA draft non è importabile/esportabile come `.cml`
- Markdown è documento di output, non artefatto di validazione
- La validazione dipartimentale rimane umana/manuale
- SchoolKB non integrato
- Nessun dato studente/classe/voto deve essere inserito nella bozza
- Label `non_applicabile` → `❌ Esclusa` in UDA draft vs `❌ Non applicabile` nell'evidence panel: incoerenza minore
- Status message "proposta non vengono incluse" è fuorviante (le evidenze 'proposta' sono incluse nel Markdown)

## File modificati

- `docs/03_execution/CML-218.md`
- `report/CML-218_evidenze_uda_workflow_smoke_and_closure_gate.md`
- `docs/REPO-MOVELOG.md`

## Invarianti

- Nessuna modifica runtime
- Nessuna modifica curriculum JSON
- Nessuna modifica `.cml`/export/import
- Nessuna modifica validator/shape-test
- Nessuna modifica service-worker/manifest
- Nessuna dipendenza
- Nessun deploy
- Nessun push (CML-218S successivo)
- Nessun secret

## Verdetto

`CML_218_EVIDENZE_UDA_WORKFLOW_SMOKE_AND_CLOSURE_GATE_READY`
