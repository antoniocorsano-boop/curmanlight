# CML-143 — TO_RUNTIME_MAPPA_DATI_TRANSFORMER_DESIGN_AND_TOOL

**Stato:** Concluso.
**Verdetto:** `CML_143_TO_RUNTIME_MAPPA_DATI_TRANSFORMER_READY`

## Obiettivo

Implementare uno strato intermedio standalone `toRuntimeMappaDati()` che trasformi l'output ricco dell'adapter JSON→MAPPA_DATI in shape flat compatibile con `renderMappaDisciplinare()`.

## Stato di partenza

```
Branch:        main
HEAD:          b55cda1 (6 ahead of origin)
origin/main:   85bbab4
Working tree:  pulito (tmp-*.json rimossi)
Validatore:    7/94, overallValid: true ✅
Contratto:     Opzione B+D — toRuntimeMappaDati() intermedio, pilota Tecnologia
```

## Tool creato

`tools/to-runtime-mappa-dati-transformer.mjs`

### Funzioni implementate

| Funzione                                | Logica                                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `readStdin()`                           | Legge stdin (pipe da adapter)                                                                    |
| `buildDescrizione(unita)`               | Descrizione sintetica da count + titoli + classi/fasce                                           |
| `buildDescrizioneProgressione(unita)`   | Descrizione breve per progressioneVerticale                                                      |
| `transformStruttureSostanziali(items)`  | `{nome, unita[]}` → `{nome, descrizione, fonte}`                                                 |
| `transformNodiDisciplinari(items)`      | `{etichetta, unita[]}` → `{etichetta, tipo="sostanziale", descrizione, fonte}`                   |
| `transformProgressioneVerticale(items)` | `{ordine, classe, fascia, unita[]}` → `{ordine, classe, fascia, descrizioneProgressione, fonte}` |
| `transformDecisioniCurricolari(items)`  | Pass-through                                                                                     |
| `toRuntimeMappaDati(data)`              | Compone l'oggetto output completo                                                                |

### Shape input (da adapter)

| Area                      | Shape input                                                                |
| ------------------------- | -------------------------------------------------------------------------- |
| `struttureSostanziali[]`  | `{nome, unita: [{id, titolo, ordine, classe, fascia, nucleo}]}`            |
| `nodiDisciplinari[]`      | `{etichetta, unita: [...]}`                                                |
| `progressioneVerticale[]` | `{ordine, classe, fascia, unita: [...]}`                                   |
| `decisioniCurricolari[]`  | `{id, tipo, nodiRiferimento, unitaRiferimento, motivazione, stato, fonte}` |

### Shape output (per runtime)

| Area                      | Shape output                                                                              |
| ------------------------- | ----------------------------------------------------------------------------------------- |
| `struttureSostanziali[]`  | `{nome, descrizione, fonte}`                                                              |
| `nodiDisciplinari[]`      | `{etichetta, tipo: "sostanziale", descrizione, fonte}`                                    |
| `progressioneVerticale[]` | `{ordine, classe\|fascia, descrizioneProgressione, fonte}`                                |
| `decisioniCurricolari[]`  | `{id, tipo, nodiRiferimento, unitaRiferimento, motivazione, stato, fonte}` (pass-through) |

### Uso

```bash
node tools/json-to-mappa-dati-adapter.mjs content/curriculum/tecnologia.normalized.json \
  | node tools/to-runtime-mappa-dati-transformer.mjs
```

## Discipline testate

| Disciplina | Strutture | Nodi | Progressioni | Decisioni | Shape OK |
| ---------- | :-------: | :--: | :----------: | :-------: | :------: |
| Tecnologia |     6     |  6   |      10      |     2     |    ✅    |
| Italiano   |    12     |  6   |      10      |     4     |    ✅    |
| Matematica |    12     |  5   |      6       |     4     |    ✅    |

## Validazioni

- Validatore: 7/94, overallValid: true ✅
- Transformer output: JSON valido per tutte ✅
- File temporanei rimossi ✅
- Runtime non modificato ✅
- JSON disciplinari non modificati ✅
- Adapter non modificato ✅
- Whitespace OK ✅

## Limiti noti

1. `descrizione` generata automaticamente — meno curata delle descrizioni scritte a mano in MAPPA_DATI hardcoded
2. `nodiRiferimento` assenti (erano già vuoti in hardcoded Tecnologia)
3. `fonte` statica `"JSON curriculum strutturato"` — non riporta gli ID unità specifici
4. Transformer standalone — non ancora integrato nel runtime

## Prossimo passo

CML-143A — Runtime Shape Transformer Output Smoke: confrontare l'output flat con lo shape effettivamente letto da `renderMappaDisciplinare()` prima di qualunque integrazione.
