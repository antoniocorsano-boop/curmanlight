# CML-056 — Role Access Code Gating Implementation

## Branch

cml-008r-fix-markdown-decision-summary

## Commit iniziale

7df3cb4 (dopo CML-056A)

## Collegamento con slice precedenti

CML-056A ha recuperato la sintassi del runtime. CML-056 introduce il gating per ruolo con codice operativo su funzioni riservate al dipartimento e al referente.

## Obiettivo

Implementare un gating con codice operativo per proteggere le funzioni riservate al dipartimento e al referente, evitando usi accidentali senza compromettere la semplicità del file unico.

## Funzioni aggiunte

- `isRoleAccessGranted()` - verifica lo stato di sblocco in sessionStorage
- `updateRoleAccessUi()` - mostra/nasconde i pulsanti di blocco
- `showRoleAccessModal(actionId, callback)` - mostra la modale per inserire il codice
- `closeRoleAccessModal()` - chiude la modale
- `lockRoleAccess()` - revoca lo sblocco
- `requireRoleAccess(actionId, callback)` - helper centralizzato per il gating

## Azioni protette

| Azione | Descrizione |
|---|---|
| `departmentOutcomeExport` | Esporta esito dipartimento .cml |
| `referentOutcomeImport` | Importa esiti dipartimentali .cml |
| `referentReportGeneration` | Scarica report gruppo di lavoro |

## Configurazione

- Codice: `CML2025` (costante, mai salvato)
- Storage: `sessionStorage.roleAccessGranted` (session-only)
- Nessun localStorage aggiunto

## Comportamento

- Prima dell'azione protetta: richiesta codice via modale
- Codice errato: errore visibile, ripetibile
- Codice corretto: esecuzione + pulsante "Blocca di nuovo"
- Refresh nella stessa scheda: mantiene lo sblocco
- "Blocca di nuovo": revoca e nasconde i pulsanti

## Microcopy

```text
Codice operativo richiesto
Questa funzione è riservata al ruolo indicato nella prova controllata. Inserisci il codice operativo fornito dal referente.
Il codice evita usi accidentali dello strumento. Non è una password istituzionale e non sostituisce autorizzazioni, firme o delibere.
```

## File modificati

- `_published_snapshot/netlify-current/index.html`

## Verifiche eseguite

- Parser JavaScript inline: PASS
- Smoke T01–T13: PASS
- Regressione `.cml`: PASS
- Nessun deploy
- Nessun nuovo localStorage

## Verdetto finale

CML_056_ROLE_ACCESS_CODE_GATING_IMPLEMENTATION_READY

## Prossimo step

CML-057 — aggiornamento report e documentazione.