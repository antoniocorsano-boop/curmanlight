# CML-430 — User Validation Intake Summary Template

## Type

docs-only

## Macro-program

PM-09 Validazione con utenti

## Stato

READY_LOCAL_NOT_PUSHED

## Pre-check Git

```
$ git status --short --branch
## main...origin/main
```

Solo untracked preesistenti. HEAD == origin/main == bcb9a0c.

```
$ git diff --check
```

Nessun output (OK).

## Validatori

| Validatore | Esito |
|---|---|
| `validate-cml-normalized-curriculum.mjs` | 14/14 PASS, overallValid true |
| `test-runtime-mappa-dati-shape.mjs` | 14/14 PASS |

## Contenuto

Il modello operativo per sintetizzare le evidenze raccolte dai docenti dopo la prova pilota è in `report/CML-430_user_validation_intake_summary_template.md`.

### Sezioni

1. Scopo del modello
2. Dati minimi da registrare
3. Tabella di sintesi evidenze
4. Schema di sintesi finale
5. Criteri di decisione
6. Priorità
7. Output atteso
8. Allineamento stato progetto

## Microfix runtime

Nessuno.

## Verdetto

CML_430_USER_VALIDATION_INTAKE_SUMMARY_TEMPLATE_READY_LOCAL_NOT_PUSHED
