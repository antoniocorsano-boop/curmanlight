# CML-351 — Post-CML-350 UI Consistency Audit and Next Microfix Selection

## Mandatory Slice Header
- Macroprogramma: PM-07 Uniformità
- Backlog: UX-024 (componenti comuni non uniformi)
- Dipende da: CML-350 (PM-07 Cross-Tab Button Uniformity Microfix)
- Aggiorna: `PRODUCT-MATURITY-PROGRESS.md`
- Tipo slice: docs-only (audit + selezione)

## Contesto
CML-350 ha uniformato a `8px` il `border-radius` di quattro classi pulsante cross-tab (`.filter-btn`, `.export-btn`, `.act`, `.add-btn`). Questa slice esegue un audit leggero post-CML-350 per individuare il prossimo micro-intervento UI/UX a basso rischio.

## Perimetro
- Audit CSS in sola lettura su `_published_snapshot/netlify-current/index.html`
- Classificazione rilievi P0-P3/HOLD
- Selezione di una sola prossima slice candidata
- Nessuna modifica runtime

## Metodo
- Ricerca pattern `border-radius:\d+px` nel CSS runtime (243 occorrenze trovate)
- Analisi per cluster di valore (5px, 6px, 7px, 8px, 9px, 10-16px, 999px)
- Classificazione per tipo di componente (pulsante, container, badge, pill, card)
- Valutazione rischio e reversibilità

## Rilievi classificati

### P0: blocco funzionale o regressione evidente
- Nessuno

### P1: incoerenza UI significativa e frequente
- Nessuno

### P2: micro-incoerenza visiva a basso rischio

**Cluster A — Pulsanti/elementi interattivi con `border-radius:7px` (residui post-CML-350):**
- `.department-import-btn` (7px)
- `.department-import-stat` (7px)
- `.local-save-btn` (7px)
- `.readonly-order-note` (7px)
- `.gap-header` (7px)
- `.gap-header-unique` (7px)
- `.role-access-warning` (7px)
- `.role-access-error` (7px)
- `.grp-hd` (7px)
- `.act-card` (7px)
- `.norm-cat-title` (7px)
- `.annual-draft-field select/input/textarea` (7px)
- `.annual-draft-units` (7px)
- `.annual-draft-btn` (7px)
- `.annual-draft-status` (7px)
- `.annual-draft-privacy` (7px)
- `.uda-smart-library-filters` (7px)
- `.uda-smart-library-field input/select/textarea` (7px)
- `.uda-smart-library-btn` (7px)
- `.uda-smart-library-status` (7px)
- `.uda-smart-library-preview` (7px)
- `.uda-smart-library-markdown` (7px)
- `.uda-smart-library-card` (7px)
- `.uda-smart-library-empty` (7px)
- `.uda-smart-library-privacy` (7px)
- `.mappa-disc-btn` inline style ×14 (7px)

**Cluster B — Elementi con `border-radius:6px` (residui):**
- `.stat` (6px)
- `.department-import-warning` (6px)
- `.department-file-row` (6px)
- `.ordine-hd` (6px)
- `.norm-link` (6px)
- `.orientation-note` (6px)
- `.curricolo-riepilogo-item` (6px)
- `.home-recommended-callout-btn` (6px)
- `.didattica-uda-draft-notice` (6px)
- `.didattica-uda-draft-status` (6px)
- `.annual-draft-unit` (6px)

**Cluster C — Elementi con `border-radius:5px` (residui minori):**
- `.norm-badge` (5px)
- `.disc-md-preview` (5px)
- `.pending-action` (5px)

### P3: miglioramento opzionale
- Card Home con `border-radius:11px` vs standard container `9px` (`.home-card`, `.home-discipline-panel`, `.home-context-card`, `.home-applicability-card`)
- Pannelli speciali con `border-radius:12px` vs standard `9px` (`.curricolo-completeness`, `.curricolo-readiness`, `.tecnologia-norm`, `.home-microguide`, `.quick-info-bar`, `.orientation-card`, `.cruscotto`)

### HOLD: non intervenire ora
- **Pill shape intenzionali** (`border-radius:16px-999px`): `.curricolo-index-pill` (18px), `.tecnologia-norm-filter` (16px), `.didattica-evidence-filter` (16px), `.evidence-reset-btn` (16px), `.save-chip` (999px), `.cruscotto-btn` (999px), `.cruscotto-toggle` (999px), `.header-toggle` (999px), `.orientation-step` (999px), `.home-inline-actions button` (999px), `.dimmed` (999px), `.badge-preparing` (999px), `.esport-role-badge` (999px), `.home-card-badge` (999px), `.gen-tag` (999px), `.quick-summary-row .profile-summary` (999px), `.evidence-btn` (12px), `.didattica-programmazione-pill` (12px)
- **Container/box standard** (`border-radius:9px`): `.toolbar`, `.aside-box`, `.usage-notice`, `.local-save-bar`, `.sum-box`, `.norm-intro`, `.norm-card`, `.disc-export-panel`, `.department-import-panel`, `.referent-validation-panel`, `.export-group`, `.tool-group`, `.esport-group`, `.guida-card`, `.guida-note`, `.notice-box`, `.didattica-evidence-box`, `.didattica-uda-box`, `.didattica-programmazione-box`, `.home-footer`, `.curricolo-path`, `.didattica-path`, `.curricolo-copertura`, `.curricolo-disc`, `.curricolo-state`, `.curricolo-index`, `.curricolo-quadro`, `.curricolo-riepilogo`, `.curricolo-ref`, `.curricolo-note`, `.home-process-path`, `.tab-generali .gen-card`, `.tab-generali .gen-note`, `.install-hint`, `.norm-warning`, `.didattica-evidence-unit`, `.didattica-uda-card`, `.didattica-uda-draft-section`, `.annual-draft-box`, `.annual-draft-preview`, `.uda-smart-library-section` — coerenti tra loro, non modificare
- **Dialog/modali** (`border-radius:14-16px`): `.role-access-dialog` (14px), `.work-context-dialog` (14px), `.mobile-menu-panel` (14px), settings overlay (16px), welcome overlay (16px), install guide (16px) — design intenzionale

## Prossima slice raccomandata

**CML-352 — Residual Button Border-Radius Uniformity Microfix**

- **Tipo**: runtime microfix (solo CSS)
- **Scope**: uniformare a `8px` i selettori CSS con `border-radius:7px` e `border-radius:6px` che sono pulsanti/bottoni/elementi interattivi (non container), estendendo il lavoro di CML-350
- **Selettori target**: ~37 selettori CSS (26 con 7px + 11 con 6px) + 14 inline style `.mappa-disc-btn`
- **Rischio**: nullo (solo CSS, nessuna logica, nessun dato)
- **Reversibilità**: totale (basta revert del commit)
- **Verificabilità**: diff CSS + smoke visivo
- **Impatto**: nessuno su dati, export, import, schema `.cml`, logica JS

## Fuori perimetro
- Nessuna modifica runtime
- Nessuna modifica HTML, JS, dati, import/export, `.cml`
- Nessuna modifica workflow Pages, service worker, localStorage/sessionStorage
- Nessuna nuova dipendenza, build, backend, API o OAuth
- `CurManLightBrain/` fuori scope

## Branch
`main` (audit docs-only, nessun branch dedicato necessario)

## Commit iniziale
`2227581` (chiusura CML-350 su `main`)

## Controlli
- `git diff --check`: PASS
- `git status --short`: pulito (solo `CurManLightBrain/` untracked)
- File runtime non modificati: PASS
- Nessun file fuori perimetro: PASS

## Verdict
`CML_351_POST_CML350_UI_CONSISTENCY_AUDIT_READY_LOCAL_NOT_PUSHED`