# CML-344 — Mappa Disciplinare Quick Selector Coherence Fix

## 1. Stato iniziale
- Branch: main
- Stato rispetto a origin/main: allineato
- Working tree: modificato locale su runtime pair (`index.html` + `_published_snapshot/netlify-current/index.html`)
- Slice precedente: CML-343
- Vincoli rispettati: intervento limitato al runtime pair e documentazione; nessuna modifica a dati, export/import, workflow ruoli, persistenza o schema `.cml`; nessun push; nessun deploy.

## 2. Obiettivo
Chiudere il residuo di coerenza del quick selector in Mappa disciplinare per la disciplina Tecnologia, garantendo presenza marker e sincronizzazione stato attivo con la disciplina visualizzata.

## 3. Evidenza tecnica runtime
- Marker confermato nel pulsante Tecnologia:
  - `data-mappa="tecnologia"`
- Handler coerente con il pattern dei pulsanti:
  - `onclick="setMappaDisciplina('tecnologia',this)"`
- Sincronizzazione attivo/disattivo confermata:
  - funzione `syncMappaDisciplinaButtons()` presente;
  - chiamata della funzione confermata nei punti di render/aggiornamento.
- Runtime pair verificata:
  - `index.html`
  - `_published_snapshot/netlify-current/index.html`

## 4. Non-obiettivi
- nessuna modifica a `content/curriculum/`
- nessuna modifica export/import
- nessuna modifica workflow ruoli
- nessuna modifica persistenza browser
- nessuna modifica schema `.cml`

## 5. Verifiche
- `git diff --check`: PASS (nessun errore)
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS (`overallValid: true`, `invalidCount: 0`)
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS (`overall: PASS`, `14 passed, 0 failed`)
- marker selector: PASS (`data-mappa="tecnologia"` e `syncMappaDisciplinaButtons()` presenti su root+snapshot)
- typo check: PASS (assenza refusi lessicali target)

## 6. Esito
- Problema risolto: quick selector Mappa disciplinare coerente per Tecnologia.
- Italiano non resta attivo quando la disciplina corrente e Tecnologia (sincronizzazione pulsanti confermata).
- Stato PM-06: 60%
- Push: completato su `main` (commit `bc7524d2518a623dda8031a029210b83380ae184`).
- Deploy Pages: completato con successo dopo rerun (`28695766095`).
- Deploy manuale: non eseguito.

## 7. Nota rilascio pubblico
- Primo controllo pubblico post-push negativo per marker non allineato.
- Root cause: deploy GitHub Pages fallito/stuck lato piattaforma, non regressione runtime.
- Azione: cancellazione run bloccato + nuovo `workflow_dispatch`.
- Verifica finale pubblica: marker `data-mappa="tecnologia"` e `syncMappaDisciplinaButtons()` presenti.

## 8. Verdict finale
`CML_344_MAPPA_DISCIPLINARE_QUICK_SELECTOR_COHERENCE_FIX_PUSHED_AND_PUBLISHED`
