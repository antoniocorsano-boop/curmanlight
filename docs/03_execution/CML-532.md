# CML-532 - Offline Service Worker Regression Check

## Stato

| Campo | Valore |
|-------|--------|
| Stato | READY_LOCAL_NOT_PUSHED |
| Branch | `main` |
| Base locale | `39ef8d915996b40162b6cac275781ede8e5b587b` |
| Data | 2026-07-17 |
| Tipo | tooling / regression check |

## Obiettivo

Aggiungere un controllo automatico dedicato al service worker, senza modificare runtime, per impedire regressioni su `APP_SHELL`, percorso canonico del motto, risorse opzionali rimosse dal precache, fallback 503/504, sync tra le due copie `sw.js` e percorsi `respondWith()` che potrebbero risolversi a `undefined`.

## Perimetro

### File autorizzati

- `tools/check-service-worker-offline-regression.mjs`
- `docs/03_execution/CML-532.md`
- `report/CML-532_offline_service_worker_regression_check.md`
- `docs/02_system/PROJECT-STATE.md`
- `docs/REPO-MOVELOG-v2.md`

### File esclusi

- `sw.js`
- `_published_snapshot/netlify-current/sw.js`
- `index.html`
- `_published_snapshot/netlify-current/index.html`
- `curman-react/**`
- `content/curriculum/**`
- `.github/workflows/**`
- `docs/REPO-MOVELOG.md` (archivio legacy, non riscritto)

## TDD

RED iniziale:

```powershell
node tools/check-service-worker-offline-regression.mjs
```

Esito atteso e osservato: fallimento `MODULE_NOT_FOUND`, perche il controllo dedicato non esisteva ancora.

GREEN:

```powershell
node tools/check-service-worker-offline-regression.mjs
```

Esito: PASS.

## Controllo aggiunto

`tools/check-service-worker-offline-regression.mjs` verifica:

1. sincronizzazione byte-for-byte tra `sw.js` e `_published_snapshot/netlify-current/sw.js`;
2. `APP_SHELL` minima esatta:
   - `./`;
   - `./index.html`;
   - `./manifest.webmanifest`;
   - `./motto-non-multa-sed-multum/`;
   - `./icons/icon-192.png`;
   - `./icons/icon-512.png`;
3. assenza delle risorse opzionali rimosse dal precache:
   - `./motto-non-multa-sed-multum.html`;
   - `./riferimenti_istituzionali_normativa.json`;
   - `./docs_distribuzione/FONTI_PTOF_RAV_NORMATIVA.md`;
   - `./docs_distribuzione/NOTE_TESTATA_ESPANDIBILE_MOBILE.txt`;
4. presenza del fallback offline CML-529;
5. installazione resiliente quando `cache.add()` fallisce per tutte le risorse;
6. navigazione offline/cache vuota con `Response` HTTP 503;
7. asset offline/cache vuota con `Response` HTTP 504;
8. assenza di `respondWith(undefined)` nei percorsi simulati.

## Controlli eseguiti

| Comando | Scopo | Esito |
|---------|-------|-------|
| `node tools/check-service-worker-offline-regression.mjs` prima della creazione | RED TDD | FAIL atteso, script mancante |
| `node tools/check-service-worker-offline-regression.mjs` | Regression check SW | PASS |
| `node --check tools/check-service-worker-offline-regression.mjs` | Sintassi script | PASS |
| `node --check sw.js` | Sintassi runtime root | PASS |
| `node --check _published_snapshot/netlify-current/sw.js` | Sintassi runtime published | PASS |
| `node tools/check-app-pair.mjs` | Sync coppia app/SW | PASS |
| `git diff --check` | Whitespace/conflict markers | PASS, solo warning CRLF non bloccanti |

## Release-gate GitHub Pages

| Campo | Valore |
|-------|--------|
| Push su branch dedicato | non eseguito |
| Merge o push su `main` | non eseguito |
| Percorsi trigger Pages nel lavoro locale complessivo | `_published_snapshot/netlify-current/sw.js` da CML-531 |
| Deploy Pages atteso | solo a futuro push/merge su `main` |
| Workflow Pages avviato | no |
| Pubblicazione verificata | non applicabile |

## Verdetto

```text
CML_532_OFFLINE_SERVICE_WORKER_REGRESSION_CHECK_READY_LOCAL_NOT_PUSHED
```