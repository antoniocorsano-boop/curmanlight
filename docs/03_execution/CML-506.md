# CML-506 — CML Contract Closure and Round-Trip Readiness

## Obiettivo

Chiudere i due blocchi P1 emersi nell’audit CML-505 e rendere coerente la catena Docente → Dipartimento → Referente.

## Modifiche

- il file `teacher_proposal` include ora `motivazione` e `fonte` per ogni proposta;
- la motivazione deriva dai dati del gap layer, con fallback esplicito e non vuoto;
- la fonte deriva da `sourceRefs`, con fallback locale CurManLight;
- `ProposalItem` dichiara formalmente entrambi i campi;
- `parseCmlFile` riconosce ora anche `referent_validation`;
- restano invariati schemaVersion 1.0, validazione umana obbligatoria e funzionamento locale.

## Verifica richiesta

- compilazione TypeScript e build React;
- import di un `teacher_proposal` prodotto dal builder React;
- parsing dei tre fileType riconosciuti;
- audit CML-470, CML-473 e CML-491;
- preview Cloudflare e review automatica.

## Verdetto atteso

`CML_506_CML_CONTRACT_CLOSURE_READY_FOR_REMOTE_VALIDATION`
