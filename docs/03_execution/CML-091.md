# CML-091 — DISCIPLINARY_APPROVAL_READINESS_PANEL_RUNTIME_INCREMENT

## Contesto

- Progetto: CurManLight / CML
- Branch: `main`
- Ultimo step chiuso: CML-090 — selezione UI Opzione B (pannello readiness sotto completezza)
- Stato attuale: 2 in revisione, 13 sola consultazione, 0 pronte per approvazione

## Obiettivo

Aggiungere pannello "Readiness per approvazione" read-only sotto la sezione "Stato di completezza" in Curriculum > Consultazione, chiarendo che la completezza non equivale ad approvazione.

## Intervento runtime

File modificato: `_published_snapshot/netlify-current/index.html`

**CSS** (dopo linea 664, prima di `.curricolo-nextaction`):
- 16 nuove righe CSS per classi `.curricolo-readiness-*`, `.readiness-counter-*`, `.readiness-badge-*`, `.readiness-row`, `.readiness-note`
- Palette azzurra per "In revisione", ambra per "Sola consultazione", grigia per "Pronte per approvazione"

**JS HTML** (dopo linea 4537, prima di `ordinii.forEach`):
- Pannello con titolo, sottotitolo, 3 contatori, 3 righe discipline, nota prudenziale

## Posizione del pannello

Subito dopo la sezione "Stato di completezza dei curricoli disciplinari", prima dell'elenco delle discipline per ordine.

## Microcopy usata

- "Readiness per approvazione" (titolo)
- "La completezza della bozza non equivale ad approvazione." (sottotitolo)
- "In revisione" / "Sola consultazione" / "Pronte per approvazione" (contatori)
- "Bozza completa, in revisione" (Tecnologia, Italiano)
- "Struttura da completare prima della readiness" (altre 13)
- "L'approvazione resta esterna allo strumento e richiede validazione degli organi competenti." (nota)

## Stati visualizzati

| Elemento | Stato |
|---|---|
| Tecnologia | In revisione |
| Italiano | In revisione |
| Altre 13 discipline | Solo consultazione |
| Pronte per approvazione | 0 |
| Approvate esternamente | 0 (non mostrato) |

## Vincoli rispettati

- ✅ Read-only (nessun pulsante, nessuna azione)
- ✅ Nessuna modifica dati curricolari
- ✅ Nessuna modifica schema `.cml`
- ✅ Nessuna modifica export/import
- ✅ Nessuna modifica role-access
- ✅ Nessuna nuova dipendenza esterna
- ✅ Nessun nuovo `localStorage`/`sessionStorage`
- ✅ Light mode only preservato
- ✅ Dark mode commentato
- ✅ Nessuna `@media` orfana
- ✅ Nessun overflow orizzontale prevedibile
- ✅ Microcopy vietata non utilizzata

## Controlli

| Controllo | Esito |
|---|---|
| `git status` — solo file consentito | ✅ |
| `git diff --check` | ✅ |
| Braces CSS bilanciate | ✅ (2120=2120) |
| Nessuna nuova dipendenza esterna | ✅ |
| Nessun nuovo `localStorage`/`sessionStorage` | ✅ |
| Dark mode commentato | ✅ |
| `@media` orfane | ✅ (nessuna) |
| Schema `.cml` invariato | ✅ |
| Export/import invariati | ✅ |
| Role-access invariato | ✅ |

## Verdetto

`CML_091_DISCIPLINARY_APPROVAL_READINESS_PANEL_RUNTIME_INCREMENT_READY`
