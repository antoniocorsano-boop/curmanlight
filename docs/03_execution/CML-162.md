# CML-162 — GEOGRAFIA_RUNTIME_MAPPA_DATI_INTEGRATION

Data: 2026-06-26

## Stato iniziale

- Root Git: `C:/Users/anton/CurManLight`
- Branch: `main`
- Commit iniziale: `0bffd68` (sync con `origin/main`)
- origin/main: `0bffd68`
- Working tree iniziale: pulito
- Push: non eseguito
- Deploy: non eseguito

## Baseline tecnica

Commit precedente: `0bffd68` (docs: audit didattica coverage gap)
Runtime mappa pre-esistente: Tecnologia, Matematica, Italiano, Scienze, Storia

## Perimetro

File autorizzati:

- `_published_snapshot/netlify-current/index.html`
- `docs/03_execution/CML-162.md`
- `report/CML-162_geografia_runtime_mappa_dati_integration.md`
- `docs/REPO-MOVELOG.md`

Non sono state modificate: schema `.cml`, export/import, service worker, validatori, dipendenze, root `index.html`, landing UI/UX, contenuti normalizzati JSON, tools, evidenze/UDA, funzioni generatrici.

## Modifiche eseguite

In `_published_snapshot/netlify-current/index.html`:

- aggiunto il pulsante `Geografia` nel selettore mappa disciplinare (tra Matematica e Storia);
- aggiunta la variabile runtime `GEOGRAFIA_MAPPA_DATI`;
- aggiunto il ramo `geografia` in `setMappaDisciplina()`.

La struttura `GEOGRAFIA_MAPPA_DATI` contiene:

- 12 strutture sostanziali;
- 3 nodi disciplinari;
- 9 progressioni verticali;
- 0 decisioni curricolari.

`D=0` è accettato: la shape runtime passa (PASS, 7/7) e non sono state inventate decisioni curricolari fittizie. Geografia non ha ancora un audit preparatorio analogo a CML-157C per Storia.

## File modificati

- `_published_snapshot/netlify-current/index.html` (8 insertions, 2 deletions)

## Validazioni eseguite

- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS, 7 file / 94 unità / `overallValid: true` / 0 errori
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS, 7 discipline / 0 failed (Geografia: S=12 N=3 P=9 D=0)
- verifica codifica blocco `GEOGRAFIA_MAPPA_DATI`: PASS, JSON parseable, nessun `\uFFFD`, nessun `???`, nessun `??`, accenti italiani conservati

## Conferme invarianti

- Tecnologia: invariata
- Matematica: invariata
- Italiano: invariato
- Scienze: invariato
- Storia: invariata
- Evidenze/UDA: invariate
- Schema .cml: non modificato
- Export/import: non modificato
- Service worker: non modificato
- Dipendenze: non modificate
- Landing UI/UX: non modificata

## Esito

La slice integra Geografia nel runtime locale senza estendere altre discipline e senza modifiche collaterali fuori perimetro.

Nessun deploy eseguito.
Nessun push eseguito.

Verdetto:

`CML_162_GEOGRAFIA_LOCAL_FINAL_AUDIT_READY`
