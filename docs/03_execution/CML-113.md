# CML-113 — VALIDAZIONE_DIPARTIMENTALE_WORKFLOW_AUDIT

## Contesto

- Progetto: CurManLight / CML
- Branch: `main`
- Commit iniziale: `1804ac5`
- Working tree: modificata su `index.html` (non stageata)
- Slice precedente chiusa: `CML_112A_HASH_SELECTION_SMOKE_TEST_PASSED`
- Stato disciplinare: **14/14 complete**
- Nuove normalizzazioni: non prioritarie

## Modalità

- **Audit-only**: nessuna modifica runtime, nessun deploy
- **Docs-only**: documentazione, report, movelog
- **Non modificati**: `index.html`, `sw.js`, `_headers`, `_published_snapshot/**`, schema `.cml`, asset, PDF, workflow Pages

## Sintesi flusso attuale

- Esiste già un pannello di validazione dipartimentale per disciplina (`curricolo-validation`) nel tab `Revisione` (`#tab-lavoro`).
- Sono già presenti 3 esiti validazione: `validata`, `validata_con_note`, `rinviata`.
- Esistono funzioni JS dedicate: `setDisciplineValidation`, `saveValidation`, `resetDisciplineValidation`, `getDepartmentValidationState`, `saveDepartmentValidationState`.
- Persistenza locale su chiave `curmanlight.departmentValidation` (localStorage).
- Esiste un secondo pannello "Validazione dipartimentale" (`#department-import-panel`) per import/esport `.cml` delle proposte docente — nome fuorviante.
- I pannelli statici "Stato di completezza" e "Readiness per approvazione" sono hardcoded e non sincronizzati con lo stato validazione.
- Il tab `Definitivo` mostra solo elementi approvati individualmente, senza stato di validazione dipartimentale.

## Problemi individuati

### P0

- Il pannello di validazione è sepolto in `#tab-lavoro` e non è visibile da `Consultazione` o `Definitivo`.
- Nessuna traccia UI tracciabile che distingua "valutazione dipartimentale" da "approvazione formale".

### P1

- Contatori statici (completezza/readiness) non alimentati dal motore validazione.
- I due pannelli "Validazione dipartimentale" (validazione vs import proposte) creano ambiguità.
- Non esiste un flusso "Pronto per approvazione" prima del `Definitivo`.

### P2

- Microcopy confusa: stesso nome per azioni diverse.
- Sotto-titolo "Readiness per approvazione" è tecnico.

### P3

- Pannelli statici lunghi senza collasso.

## Stati consigliati

- `in_revisione`
- `validata_dipartimento` (validata)
- `validata_con_note`
- `rinviata`
- `pronto_approvazione`
- `approvato_esternamente`

## Azioni consentite

- Registrare esito per disciplina (validata / con note / rinviata)
- Aggiungere note opzionali
- Revocare validazione
- Visualizzare stato aggiornato

## Azioni vietate

- Nessun pulsante "Approva definitivamente"
- Nessuna modifica contenuti
- Nessun automatismo di promozione
- Nessun dato personale obbligatorio
- Nessuna autenticazione reale

## Posizione UI consigliata

- Portare il pannello validazione anche in `Consultazione` sotto i pannelli statici.
- Separare i due pannelli in `Revisione`: "Validazione dipartimentale disciplina" vs "Import/Export proposte docente".
- Sincronizzare i contatori statici con lo stato validazione.

## Primo incremento CML-114

- Posizionare la logica di validazione anche nel tab `Consultazione`.
- Sincronizzare i due pannelli statici (completezza/readiness) con lo stato `departmentValidation`.
- Separare microcopy e titoli per evitare sovrapposizioni.
- Riesumare struttura dati/JS già presente senza duplicati.

## File da modificare

- `_published_snapshot/netlify-current/index.html`

## File da non modificare

- `sw.js`, `_headers`, asset, `content/**`, `.github/**`, PDF, manifest

## Controlli minimi

- Pannello visibile in più tab senza rompere il layout.
- Storage sincronizzato senza perdita dati.
- Contatori aggiornati dinamicamente.
- Nessuna regressione role-access, import/export, responsive.

## Verdetto

`CML_113_VALIDAZIONE_DIPARTIMENTALE_WORKFLOW_AUDIT_READY`
