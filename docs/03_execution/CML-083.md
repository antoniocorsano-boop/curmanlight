# CML-083 — DESIGN_SYSTEM_DARK_MODE_CONTRAST_ALIGNMENT_RUNTIME_FIX

## Contesto
CML-078 ha introdotto variabili CSS, dark mode automatica e normalizzazione iconografica. Step successivi (CML-079/079A microguida, CML-081/081A Curriculum, CML-082/082A Didattica) hanno aggiunto contenuti senza allineare completamente il dark mode alle nuove classi Didattica.

## Problema osservato
In modalità scura l'area Didattica risultava compromessa:
- Testo principale delle card (`color:#333` inline) invisibile su sfondo `#252525`
- Sezioni `.didattica-evidence-section ul` con `color:#333` hardcoded, illeggibile in dark mode
- Titoli sezione `.didattica-evidence-section-title` (`#00695c`) con contrasto insufficiente
- `.didattica-uda-card-sub` (`#5e35b1`) troppo scuro su sfondo scuro
- `.didattica-uda-box` senza dark mode override (sfondo lavanda chiaro)
- `.didattica-uda-field-value ul` con `color:#333` hardcoded

## Causa individuata
Le classi CSS specifiche dell'area Didattica non erano state incluse nel blocco `@media(prefers-color-scheme:dark){}` quando sono state introdotte. Il JS di `renderDidattica()` usava `color:#333` inline.

## Fix applicato
1. **JS** (linea 1914): rimosso `color:#333;` dall'inline style del `<p>` traguardo — ora eredita `var(--cml-text)` dal body
2. **CSS dark mode** (7 regole aggiunte prima della chiusura del blocco):
   - `.didattica-evidence-section ul,.didattica-evidence-section p` → `var(--cml-text)`
   - `.didattica-uda-card-sub` → `#b39ddb` (lilla leggibile)
   - `.didattica-evidence-section-title` → `#80cbc4` (teal chiaro)
   - `.didattica-uda-field-value ul` → `var(--cml-text)`
   - `.didattica-uda-box` → sfondo `#2a1a2a`, bordo `#7b1fa2`, testo `#ce93d8`
   - `.didattica-uda-box strong` → `#e1bee7`
   - `.didattica-uda-field` → bordo `var(--cml-divider)`

## Perimetro
- Solo file `_published_snapshot/netlify-current/index.html`
- 8 inserzioni, 1 cancellazione
- Nessuna nuova funzione, nessuna modifica logica JS

## Vincoli rispettati
- ✅ Nessuna nuova funzione
- ✅ Nessuna modifica schema `.cml`
- ✅ Nessuna modifica export/import docente, dipartimento o referente
- ✅ Nessuna modifica role-access gating o codice operativo
- ✅ Nessuna modifica `localStorage`/`sessionStorage`
- ✅ Nessun backend, login, OAuth, API, database, cloud o autenticazione
- ✅ Nessuna dipendenza esterna (Tailwind, font remoti, CDN, immagini remote)
- ✅ Nessun toggle manuale dark mode — solo `@media(prefers-color-scheme:dark)`
- ✅ Aree Curriculum e altre sezioni non modificate

## Controlli eseguiti
- ✅ `git diff --check`: nessun whitespace error
- ✅ CSS braces bilanciate: 969 `{` = 969 `}`
- ✅ Nessuna `@media` orfana
- ✅ Dark mode solo con `@media(prefers-color-scheme:dark)`
- ✅ Nessuna nuova dipendenza (tailwind, fonts.googleapis, fonts.gstatic, https://cdn)
- ✅ Nessun nuovo uso di `localStorage`/`sessionStorage`
- ✅ Schema `.cml`, export/import, role-access invariati

## Verdetto
`CML_083_DESIGN_SYSTEM_DARK_MODE_CONTRAST_ALIGNMENT_READY`

## Prossimo step raccomandato
CML-083A — DESIGN_SYSTEM_DARK_MODE_CONTRAST_LIVE_SMOKE:
- Verifica live su `https://antoniocorsano-boop.github.io/curmanlight/`
- Testare Home, Didattica, Curriculum, Esportazioni, Guida in light/dark mode
- Desktop e mobile, nessun overflow orizzontale
