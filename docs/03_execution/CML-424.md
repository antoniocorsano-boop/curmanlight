# CML-424 — Work Context and School Year Runtime Patch

- Macroprogramma: PM-06 (Accompagnamento), PM-05 (Esperienza di lavoro)
- Backlog: —
- Dipende da: CML-421G, CML-423, CML-423A
- Aggiorna: PRODUCT-MATURITY-PROGRESS.md

## Obiettivo

Implementare il campo Anno scolastico nel profilo utente e il chip contesto sintetico nel runtime, con salvataggio locale retrocompatibile e inclusione nella bozza UDA.

## Cambiamenti

### Runtime (index.html e _published_snapshot/netlify-current/index.html)

1. **DEFAULT_PROFILE esteso**: aggiunto `annoScolastico:""` — retrocompatibile via `{...DEFAULT_PROFILE,...payload.profile}`
2. **Helper functions** (`getSchoolYearLabel`, `getWorkContextChip`, `requiresSchoolYearMessage`) — aggiunte dopo `disabledFor()`
3. **Campo UI "Anno scolastico"** — campo di testo libero (placeholder "2026/27") nel pannello Impostazioni, tra Disciplina e Ordine
4. **Popolamento automatico** — `value="${escapeHtml(userProfile.annoScolastico||"")}"` nel template
5. **Salvataggio** — `const annoScolastico=...` letto da `#profile-anno-scolastico` e incluso in `userProfile`
6. **Chip contesto sintetico** — `renderProfileSummary()` ora usa `getWorkContextChip()` che produce: `Disciplina · Ordine · A.S. YYYY/YY` (o "Anno scolastico non impostato")
7. **Bozza UDA** — aggiunta riga `**Anno scolastico:** A.S. YYYY/YY` dopo la riga Ordine

### Non modificati

- Dati curricolari (content/curriculum/)
- Flusso .cml
- Backup/export (inclusione automatica via profile:userProfile)
- Navigazione generale
- Deploy

## Verifica

- `git diff --check`: ok (solo CRLF warning)
- Validator curriculum: 14/14 valid, 0 errori
- Runtime shape test: 14/14 passed

## Verdetto

CML-424_WORK_CONTEXT_AND_SCHOOL_YEAR_RUNTIME_PATCH_READY_UNCOMMITTED
