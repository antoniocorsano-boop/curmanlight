# CML-079: Microguida Operativa Home Runtime Increment

## Contesto

Ultimo step chiuso: CML-078A (visual affinity + dark mode smoke, commit `e5ed038`).
Obiettivo: aiutare l'utente che apre CurManLight a capire subito come usare lo strumento.

## Obiettivo

Aggiungere nella Home una microguida operativa breve, intitolata "Usa CurManLight in 5 minuti", che spiega il percorso minimo per docente, dipartimento e referente.

## Perimetro

- Solo HTML/CSS/microcopy
- Nessuna nuova funzione
- Nessuna modifica a schema `.cml`, export/import, role-access, codice operativo
- Nessun deploy in questa slice

## Modifiche

**File:** `_published_snapshot/netlify-current/index.html`

- Aggiunte classi CSS: `.home-microguide-*` (8 classi)
- Aggiunto dark mode coverage: `.home-microguide` incluso nel selettore dark
- Aggiunto HTML microguida nella Home (`.home-dashboard`), tra `.home-cards` e `.home-footer`

## Vincoli rispettati

- ✅ Nessuna nuova funzione
- ✅ Nessuna modifica schema `.cml`
- ✅ Nessuna modifica export/import
- ✅ Nessuna modifica role-access
- ✅ Nessuna modifica `sessionStorage`/`localStorage`
- ✅ Nessuna dipendenza esterna
- ✅ Nessun CDN, Tailwind, font remoto, icona remota
- ✅ Validazione umana esplicita (footer + nota)

## Controlli

- ✅ `git status`: solo `index.html` modificato
- ✅ `git diff --check`: nessun whitespace error
- ✅ No CDN/esternali introdotti
- ✅ No `localStorage`/`sessionStorage`
- ✅ Schema `.cml`, export/import, role-access invariati
- ✅ Dark mode compatibile (background via `var(--cml-card)`, testo via `var(--cml-text)`)
- ✅ Responsive: layout colonna singola su mobile (`@media max-width:700px`)
- ✅ Nessun overflow orizzontale
- ✅ Validazione umana esplicita nel footer

## Verdetto

`CML_079_MICROGUIDA_OPERATIVA_HOME_RUNTIME_INCREMENT_READY`

## Prossimo step

CML-079A — Microguida operativa home live smoke
