# CML-153 - CONTROLLED_PAGES_DEPLOY_FORCE_AND_LIVE_CONTENT_SMOKE

**Stato:** completato.
**Verdetto:** `CML_153_CONTROLLED_PAGES_DEPLOY_FORCE_AND_LIVE_CONTENT_SMOKE_READY`

## Stato iniziale

| Parametro       | Valore         |
| --------------- | -------------- |
| Branch          | `main`         |
| Commit iniziale | `eb6729f`      |
| HEAD            | `eb6729f`      |
| origin/main     | `eb6729f`      |
| Sync remoto     | gia' allineato |

`eb6729f` risultava gia' presente su `origin/main`; push non necessario.

## GitHub Pages

Workflow rilevato: `.github/workflows/pages.yml`.

- Trigger: `workflow_dispatch` e `push` su `main`, limitato ai path `_published_snapshot/netlify-current/**` e `.github/workflows/pages.yml`.
- Cartella pubblicata: `_published_snapshot/netlify-current`.
- Ultimo run automatico prima di CML-153: successo su `b3fb181`, perche' i commit successivi erano docs-only e non toccavano i path del trigger.
- Run forzato: `28166929195`, evento `workflow_dispatch`, branch `main`, headSha `eb6729f`, conclusione `success`.

Se il workflow non parte automaticamente, rilanciarlo da GitHub Actions con **Deploy CurManLight to GitHub Pages -> Run workflow -> branch main**.

## Live smoke

URL verificato: https://antoniocorsano-boop.github.io/curmanlight/?v=cml153

| Verifica                              | Esito                                        |
| ------------------------------------- | -------------------------------------------- |
| HTTP                                  | `200`                                        |
| HTML contiene `TECNOLOGIA_MAPPA_DATI` | si                                           |
| HTML contiene `GENERATA`              | si                                           |
| HTML contiene stringa empty state     | si, nel markup nascosto                      |
| Empty state visibile in browser       | no, ma il rendering mappa non viene popolato |
| Console browser                       | `SyntaxError: Unexpected identifier 'esito'` |

Sintassi localizzata nello script inline: stringa JS delimitata da apici singoli con testo `l'esito` non escapato nella sezione validazione dipartimentale. Lo script si interrompe prima di esporre `setMappaDisciplina`, `renderMappaDisciplinare` e i dataset `*_MAPPA_DATI` su `window`.

## Smoke contenuti

| Area                   | Esito                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------- |
| Tecnologia             | live aggiornato nel sorgente, ma contenuti mappa non renderizzati per SyntaxError runtime |
| Matematica             | bottone/dataset presenti nel sorgente, rendering non raggiungibile per stesso SyntaxError |
| Italiano               | bottone/dataset presenti nel sorgente, rendering non raggiungibile per stesso SyntaxError |
| Testi tecnici visibili | nessun token tecnico visibile nel contenitore mappa; il contenitore resta vuoto           |

## Service worker/cache

`_published_snapshot/netlify-current/sw.js` e live `sw.js?v=cml153` contengono ancora `curmanlight-cache-v452b422`.

Il cache-busting `?v=cml153` ha recuperato il nuovo `index.html`, quindi per questa verifica non risulta il blocco primario. Tuttavia il service worker e' ancora cache-first e puo' continuare a servire vecchie risposte per URL gia' presenti in cache, specialmente `./` o `./index.html`.

## Classificazione

**E - live aggiornato ma rendering Tecnologia ancora incompleto.**

La causa CML-151/CML-152 "deploy non aggiornato" e' superata dal workflow dispatch CML-153. La nuova causa osservata e' runtime JS parse error, non cache e non assenza dati nel live.

## Prossimo slice raccomandato

`CML-154 - TECNOLOGIA_LIVE_RUNTIME_PARSE_ERROR_MICROFIX`

Perimetro proposto: microfix runtime minimale sull'apostrofo non escapato in `index.html`, seguito da smoke browser live e valutazione separata del bump service worker/cache se necessario.

## Controlli

- `git diff --check`: da eseguire prima del commit finale.
- Runtime: invariato in CML-153.
- Generator/adapter/transformer/validator/JSON/schema `.cml`: invariati.
- `.planning/codebase/`: assente.
- `jcode/`: non usato.
- `57df9d8`: assente dalla cronologia verificata.
