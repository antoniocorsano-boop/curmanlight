# CML-428 — User Validation Pilot Kit

## Type

docs-only

## Macro-program

PM-09 Validazione con utenti

## Stato

READY_LOCAL_NOT_PUSHED

## Pre-check Git

```
$ git status --short --branch
## main...origin/main
```

Solo untracked preesistenti (`CurManLightBrain/`, `docs/03_execution/CML-418.md`, `report/screenshots/`).

```
$ git log --oneline -5
4eed0b4 docs: verify user validation entry live smoke (CML-427)
f561066 CML-400 docs-only closure: verdict PUSHED_AND_LIVE_VERIFIED
70a6f04 ux: add guided user validation entry (CML-400)
2492e7c docs: define user validation scenario pack (CML-399)
be6d81e docs: close CML-426 pushed and live verified
```

HEAD == origin/main == 4eed0b4. Working tree pulito per tracciati.

```
$ git diff --check
```

Nessun output (OK).

## Validatori

| Validatore | Esito |
|---|---|
| `validate-cml-normalized-curriculum.mjs` | 14/14 PASS, overallValid true |
| `test-runtime-mappa-dati-shape.mjs` | 14/14 PASS |

## Contenuto del kit

Il kit operativo per la prova pilota con docenti non tecnici è in `report/CML-428_user_validation_pilot_kit.md`.

### Sezioni del kit

1. Scopo della prova pilota
2. Profilo dei partecipanti
3. Messaggio di invito
4. Istruzioni operative per il docente
5. Durata suggerita
6. Cosa osservare
7. Formato di restituzione
8. Griglia interna di lettura delle evidenze
9. Criteri per trasformare osservazioni in roadmap

## Microfix runtime

Nessuno.

## Verdetto

CML_428_USER_VALIDATION_PILOT_KIT_READY_LOCAL_NOT_PUSHED
