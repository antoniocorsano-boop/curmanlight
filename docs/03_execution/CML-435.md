# CML-435 — Home Mock Conformance Visibility Gate

- Macroprogramma: PM-09 Validazione con utenti
- Dipende da: CML-434 (Home Task Selector Runtime)
- Tipo slice: runtime micro-slice (visibility gate)
- Runtime: modificato (index.html, _published_snapshot/netlify-current/index.html)
- Branch: codex/cml-435-home-mock-conformance (pushata su origin, PR #26 aperta, non mergiata)

## Obiettivo

Adeguare la Home al mock approvato, inibendo tutti i blocchi non conformi dopo l'introduzione del task selector CML-434.

Principio: la Home mostra solo ciò che appartiene al mock approvato.

## Blocchi rimossi dalla Home

1. **Seconda card "Ambiente curricolare d'Istituto"** (`.home-inline-banner`) — duplicata dal titolo nell'hub
2. **"Operazioni principali"** — card duplicate Curricolo vigente e Progetta (coperti dal task selector)
3. **"Operazioni di supporto"** — card duplicate Applicazione per classi, Adeguamento IN 2025, Processo di revisione, Esportazioni (coperti dal task selector)

## Blocchi mantenuti

- Hub `.home-process-hub` (header, task selector, context panel, percorso, pipeline, aree, governance strip)
- `.home-validation-entry` (Test guidato)
- `.home-context-card` (Contesto di lavoro)
- `.home-applicability-card` (Curricolo applicabile)
- `.home-discipline-panel` (Discipline)
- `.home-footer`

## Altre modifiche

- Etichette pulsante azione pannello contestuale CML-434: da "Azione: vai a ..." a "Apri il curricolo", "Apri Progetta", "Apri proposta", "Apri Esportazioni"
- Navigazione invariata: consulta→curricolo, progetta→didattica, proponi→lavoro, esporta→esportazioni

## Testi residui (motivati)

- "curricolo vigente" in `.home-process-step` — parte del mock approvato
- "Applicazione per classi" in `renderWorkContextSummary()` — usato nel blocco contesto di lavoro mantenuto
- "Processo di revisione" in tab Progetta — non in Home

## Validazione

- `git diff --check`: ok
- Curriculum validator: 14/14 PASS
- Runtime shape test: 14/14 PASS
- Runtime pair: allineato (27 occorrenze CML-434 markers in entrambi)

## Verdetto

CML_435_HOME_MOCK_CONFORMANCE_VISIBILITY_GATE_PUSHED_BRANCH_NOT_MERGED
