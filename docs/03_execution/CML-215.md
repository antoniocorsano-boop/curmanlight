# CML-215 - MINIMAL_EVIDENCE_PANEL_RUNTIME_INCREMENT

Data: 2026-06-27

## Obiettivo

Generalizzare il pannello Evidenze/UDA da Tecnologia-only a tutte le 14 discipline nel tab Didattica.

## Preflight

| Parametro         | Valore                         |
| ----------------- | ------------------------------ |
| Branch            | `main`                         |
| HEAD / origin     | `bd5fb02` (CML-214S)           |
| Working tree      | pulito (dopo pulizia `_tmp_*`) |
| Dati normalizzati | 14/14                          |
| Runtime mappa     | 14/14                          |
| Validatore        | 14/14 PASS                     |
| Shape test        | 14/14 PASS                     |

## Cosa è cambiato

File modificato: `_published_snapshot/netlify-current/index.html`

### Modifiche CSS (evidence-badge, evidenze azioni)

- `.evidence-badge` / `--adopted` / `--adapted` / `--excluded`: badge di stato per evidenze marcate
- `.evidence-actions`: contenitore flessibile per i bottoni Adatta/Adotta/Escludi
- `.evidence-btn` / `--adopt` / `--adapt` / `--exclude`: bottoni compatti per marcatura
- `.evidence-reset-btn`: reset globale marcature localStorage
- `.didattica-evidence-actions-bar`: barra in alto per reset

### Modifiche UI testo

- "Tecnologia" → "curricolo" nei testi informativi (2 occorrenze)
- "Evidenze e criteri indicativi" → aggiunto "Dati locali, nessun dato studente"

### Nuove funzioni JS

- `getEvidenceState(evidenceId)`: legge stato marcatura da localStorage
- `setEvidenceState(evidenceId, state)`: salva e ri-render
- `resetEvidenceStates()`: reset globale marcature
- `getDisciplineEvidenceData()`: funzione chiave — restituisce unità di apprendimento per la disciplina correntemente selezionata

### Modifiche a funzioni esistenti

- `renderDidattica()`: ora usa `getDisciplineEvidenceData()` invece di `TECNOLOGIA_NORM_DATA.unitaApprendimento`
- Ogni evidenza ha badge di stato + bottoni Adotta/Adatta/Escludi

### Nuovi dati

- `ALL_CURRICULUM_DATA`: oggetto inline con dati curricolari per tutte le 14 discipline, chiavi allineate a `mappaDisciplinaCorrente`

### Correzioni

- `arte-e-immagine` → `arte-immagine`: chiave allineata al valore di `mappaDisciplinaCorrente`
- `latino--lel-` → `latino-lel`: chiave allineata

## Perimetro rispettato

- [x] Nessuna modifica curriculum JSON
- [x] Nessuna modifica schema `.cml`
- [x] Nessuna modifica export/import
- [x] Nessun generatore UDA
- [x] Nessuna esportazione UDA
- [x] Nessun modello dati UDA persistente
- [x] Nessun dato personale/studente
- [x] localStorage solo per marcature evidenze
- [x] Nessuna modifica validator
- [x] Nessuna modifica shape-test
- [x] Nessun deploy
- [x] Nessun segreto

## Marcatura locale

Implementata: Adotta/Adatta/Escludi per evidenze, reset globale. Dati in localStorage (`cml_evidenze_state`), nessun dato personale.

## Smoke test

- Validatore: 14/14 PASS
- Shape test: 14/14 PASS
- Hash smoke: 14/14 PASS
- Evidenze panel: non più Tecnologia-only, generalizzato a 14/14

## Verdict

`CML_215_MINIMAL_EVIDENCE_PANEL_RUNTIME_INCREMENT_COMPLETE`

## Next

CML-215S push controllato, poi CML-216 UDA draft/export readiness audit.
