# CML-425 — Home D2 Context Chip Runtime Patch Contract

## Mandatory Slice Header

- Macroprogramma: PM-09 Validazione con utenti / PM-03 Orientamento / PM-05 Esperienza di lavoro / PM-06 Accompagnamento / PM-07 Uniformità
- Dipende da: CML-424, CML-421G, CML-421F, CML-422
- Tipo slice: docs/runtime-patch-contract remoto
- Branch: `cml-425-home-d2-context-chip`
- Base attesa: `main` post CML-424 (`53b07db`)
- Runtime: non modificato da questa slice documentale
- Main: non modificato

## Scopo

Preparare la prossima micro-slice runtime:

```text
Home D2 con chip Contesto di lavoro
```

CML-425 non deve implementare tutto il mock D2. Deve solo rendere la Home più coerente con il contratto visuale canonico e con il nuovo Contesto di lavoro introdotto da CML-424.

## Formula D2 applicata alla Home

```text
Home = B con cornice A
```

Significa:

```text
B: esperienza guidata per il docente
A: cornice istituzionale sobria
```

## Dipendenza tecnica da CML-424

CML-424 ha introdotto o previsto nel runtime:

```text
userProfile.annoScolastico
getSchoolYearLabel()
getWorkContextChip()
requiresSchoolYearMessage()
```

CML-425 deve riusare `getWorkContextChip()` e non duplicare logiche di contesto.

## Obiettivo UI minimo

La Home deve comunicare:

```text
che ambiente sto usando;
con quale contesto sto lavorando;
che cosa posso fare ora;
che la validazione resta umana.
```

## Struttura Home D2 minima

```text
Ambiente curricolare d'istituto
[chip contesto: Tecnologia · Scuola Secondaria di I grado · A.S. 2026/27]

Cosa vuoi fare oggi?

[Consulta il curricolo]
[Prepara una progettazione]
[Esporta un documento]
[Verifica fonti e limiti]

La validazione resta umana. L'app prepara materiali di lavoro, non approva il curricolo.
```

## Perimetro ammesso

### Dentro

```text
aggiungere/rafforzare chip contesto in Home;
riusare getWorkContextChip();
aggiungere microcopy guida "Cosa vuoi fare oggi?" se non già presente;
rafforzare le quattro azioni principali;
aggiungere avviso validazione umana breve;
mantenere navigazione esistente tramite setTab o funzioni già presenti;
```

### Fuori

```text
nuova navigazione completa;
refactor Home ampio;
nuovi export;
nuovi dati curricolari;
modifica flusso .cml;
backend/login/Drive;
nuovo design system completo;
implementazione intero mock D2;
modifica di Progettazione/Esportazione oltre ai link Home;
```

## Regola chip contesto

Usare:

```js
getWorkContextChip()
```

Output atteso se completo:

```text
Tecnologia · Scuola Secondaria di I grado · A.S. 2026/27
```

Output atteso se anno scolastico manca:

```text
Tecnologia · Scuola Secondaria di I grado · Anno scolastico non impostato
```

## Regola microcopy

Ammesso:

```text
Cosa vuoi fare oggi?
Consulta il curricolo
Prepara una progettazione
Esporta un documento
Verifica fonti e limiti
La validazione resta umana
L'app prepara materiali di lavoro, non approva il curricolo
```

Da evitare:

```text
Dashboard tecnica
Approva il curricolo
Pubblica automaticamente
Definitivo
Wiki
```

## Quattro azioni Home

### 1. Consulta il curricolo

Destinazione ammessa:

```js
setTab('curricolo')
```

### 2. Prepara una progettazione

Destinazione ammessa:

```js
setTab('didattica')
```

oppure tab già esistente equivalente.

### 3. Esporta un documento

Destinazione ammessa:

```js
setTab('esportazioni')
```

### 4. Verifica fonti e limiti

Destinazione ammessa:

```js
setTab('guida')
```

oppure se esiste una vista fonti/riferimenti più specifica, usare quella senza introdurre nuova navigazione.

## Accessibilità

Ogni card/azione deve avere:

```text
titolo chiaro;
descrizione breve;
bottone o link con label testuale;
focus navigabile;
nessuna dipendenza dal solo colore;
```

## Mobile

Su mobile:

```text
una colonna;
chip contesto breve;
quattro azioni come card/btn verticali;
avviso validazione compatto;
nessun pannello laterale;
```

## Patch plan futuro

### STEP 1 — Individuare blocco Home reale

Cercare nel runtime:

```text
home
home-inline-banner
home-discipline-status
Consulta il curricolo
setTab('curricolo')
```

### STEP 2 — Inserire chip contesto

Nel blocco Home, vicino al titolo o alla sezione introduttiva, aggiungere:

```html
<div class="home-context-chip" id="home-context-chip"></div>
```

oppure equivalente coerente con classi esistenti.

### STEP 3 — Aggiornare render Home

Nel render/refresh Home già esistente, aggiungere:

```js
var homeContextChip=document.getElementById("home-context-chip");
if(homeContextChip) homeContextChip.textContent=getWorkContextChip();
```

### STEP 4 — Quattro azioni guidate

Se sono già presenti, rinominarle/allinearle.

Se non sono presenti, aggiungere solo card minime con azioni esistenti:

```html
<button onclick="setTab('curricolo')">Consulta il curricolo</button>
<button onclick="setTab('didattica')">Prepara una progettazione</button>
<button onclick="setTab('esportazioni')">Esporta un documento</button>
<button onclick="setTab('guida')">Verifica fonti e limiti</button>
```

Non introdurre nuove funzioni di navigazione.

### STEP 5 — Validazione umana

Aggiungere microcopy breve:

```text
La validazione resta umana. L'app prepara materiali di lavoro, non approva il curricolo.
```

## Runtime pair da aggiornare quando applicato

```text
index.html
_published_snapshot/netlify-current/index.html
```

## Validazioni obbligatorie quando applicato

```text
git diff --check
validator curriculum 14/14
runtime shape test 14/14
smoke Home desktop
smoke Home mobile
smoke navigazione quattro azioni
smoke contesto con anno scolastico presente/mancante
```

## Acceptance criteria

```text
Home mostra chip contesto basato su getWorkContextChip().
Home espone quattro azioni principali in linguaggio docente.
La validazione umana è visibile ma compatta.
Nessuna nuova navigazione viene introdotta.
Nessuna modifica dati curricolari.
Nessuna modifica flusso .cml.
Runtime pair allineato quando applicato.
Mobile resta a una colonna.
```

## Decisione CML-425

```text
CML-425 è pronto come contratto di patch remoto.
Non applicare runtime dal connettore remoto se non è disponibile una patch parziale sicura.
```

## Verdict

```text
CML_425_HOME_D2_CONTEXT_CHIP_RUNTIME_PATCH_CONTRACT_READY_REMOTE_BRANCH_NOT_APPLIED
```
