# CML-461 — App Pair Divergence Authority Audit

- **Data**: 2026-07-11
- **Tipo**: docs-only / repository integrity audit / pre-runtime gate
- **Branch**: `codex/cml-461-app-pair-divergence-authority-audit`
- **Base**: `2fcad538a4fafbdfc842e42c6736f5b92dcb5f6f`

## Obiettivo

Certificare con strumenti locali tutte le differenze tra `index.html` e `_published_snapshot/netlify-current/index.html`, determinare l'autorità e definire il contratto per la riconciliazione.

## File analizzati

| File | Hash (blob) | Byte | Righe |
|------|-------------|------|-------|
| `index.html` | `b90ce3555c62f6baa3b4c9a80ccf34bf7b0c504a` | 863361 | 6824 |
| `_published_snapshot/netlify-current/index.html` | `0db23f818e92abd351fbc1115f7580289912c616` | 863305 | 6823 |

Differenza: 56 byte, 1 riga.

## Differenze

### Hunk 1 — CSS blank line (linea 751)

La copia pubblicata contiene una riga vuota dopo `.home-dashboard{...}`. La root no.

- **Classificazione**: editoriale
- **Impatto funzionale**: nessuno

### Hunk 2 — Arrow encoding (4 occorrenze, linee 1662-1682)

La root usa `→` (UTF-8 literal U+2192). La published usa `\u2192` (escape Unicode letterale). In HTML statico `\u2192` non viene interpretato come escape Unicode e verrebbe mostrato letteralmente all'utente.

- **Classificazione**: funzionale (regressione visiva)
- **Impatto funzionale**: sì — le quattro frecce mostrerebbero `\u2192` invece di `→`
- **Rettificata durante**: review PR #52, correzione applicata in CML-462

### Hunk 3 — `refreshCurrentDisciplineViews()` whitespace (linee 3832-3834)

Root: `renderMappaDisciplinare();syncMappaDisciplinaButtons();` sulla stessa riga.
Published: stesse due chiamate su righe separate con indentazione diversa.

Stesse chiamate, stesso ordine, stessi parametri, nessuna differenza di controllo di flusso.

- **Classificazione**: whitespace / formatting
- **Impatto funzionale**: nessuno

### Hunk 4 — `renderHomeWorkContext()` position (linee 6705-6795)

Root: la funzione è dichiarata prima del blocco CML-434.
Published: la funzione è dichiarata dopo il blocco CML-434.

La root include un commento extra `// disciplina from selDisc, other fields from loadWorkContext`. Il corpo operativo è identico (verificato con strip di whitespace e commenti).

JavaScript function declaration: hoisting garantisce la stessa disponibilità a runtime indipendentemente dalla posizione nel file.

- **Classificazione**: ricollocamento + commento non funzionale
- **Impatto funzionale**: nessuno

## Verifica encodifica frecce (prima della correzione)

| Copia | Frecce literal `→` | Escape `\u2192` | Totale |
|-------|---------------------|------------------|--------|
| Root | 41 | 0 | 41 |
| Published | 37 | 4 | 41 |

La snapshot pubblicata conteneva 4 occorrenze letterali `\u2192` che in HTML statico non vengono interpretate come Unicode. La root conteneva il carattere reale `→`. Questa differenza era funzionale e è stata corretta durante la review della PR #52.

## Verifica semantica renderHomeWorkContext

Corpo della funzione estratto e confrontato con strip di whitespace multipli e commenti: **identico**.

## Origine storica

| Commit | Evento | root hash | published hash | Identici? |
|--------|--------|-----------|----------------|-----------|
| `42b77d2` (CML-434) | Task selector runtime | simmetrici | simmetrici | sì |
| `17a51c5` (CML-439) | Home approved mock | `b90ce355...` | `0db23f81...` | no |
| `e40827e` (CML-441) | Home action simplification | `7ea55b45...` | `7ea55b45...` | **sì** |
| `a83fc323` (pre-PR#50) | prima del merge | `b90ce355...` | `0db23f81...` | no |
| `2fcad538` (HEAD) | PR #50 squash merge | `b90ce355...` | `0db23f81...` | no |

La divergenza corrente deriva dalla catena CML-439 ed era già presente nella mainline prima del merge della PR #50.

Un eventuale riallineamento osservato durante CML-441 non risulta consolidato nella cronologia remota che conduce alla baseline `a83fc323`.

La PR #50, non includendo modifiche a `index.html` o alla snapshot pubblicata, ha preservato la divergenza preesistente e non l'ha reintrodotta.

## Decisione di autorità

**AUTORITÀ** = `_published_snapshot/netlify-current/index.html`

Motivazioni:

1. La snapshot è la fonte effettivamente pubblicata su GitHub Pages.
2. La root è definita come copia di lavoro in `AGENTS.md`.
3. Il comportamento live è verificato dalla snapshot.
4. La snapshot non è stata modificata dalla PR #50; la divergenza preesistente è stata preservata.

## Direzione raccomandata

```
DIREZIONE FUTURA = published → root
```

Copia byte-per-byte dalla snapshot alla root. Non modifica un percorso trigger Pages. Non attiva deploy.

## Impatto GitHub Pages

Nessun deploy attivato dalla riconciliazione proposta:

- `index.html` radice non è percorso trigger.
- Il solo percorso trigger è `_published_snapshot/netlify-current/**`.
- La snapshot non viene modificata.

## Contratto CML-462

```text
CML-462 — App Pair Root Reconciliation
Tipo: runtime microfix / sync
Perimetro: solo index.html (copia dalla snapshot)
Eccezione: violazione regola "modificare entrambi i file" — justificato perché lo scopo è sincronizzare una copia divergente alla fonte autorevole invariata.
```

Criteri minimi:

```powershell
Copy-Item _published_snapshot/netlify-current/index.html index.html -Force
git diff --check
node tools/check-app-pair.mjs    # deve restituire allSync: true
node tools/validate-cml-normalized-curriculum.mjs
node tools/test-runtime-mappa-dati-shape.mjs
git diff --name-status origin/main...HEAD  # deve mostrare solo M index.html
```

Esito richiesto: `index.html: PAIR_SYNC`, `sw.js: PAIR_SYNC`, `allSync: true`.

## Rischi residui

Nessuno. La riconciliazione non altera il comportamento pubblico, non attiva Pages e non modifica dati curricolari.

## Controlli eseguiti

| Comando | Esito |
|---------|-------|
| `node tools/check-app-pair.mjs` | PASS (strumento), FAIL PREESISTENTE (coppia) — atteso |
| `git diff --no-index` | 4 hunk, tutti non-funzionali |
| `git diff --no-index --ignore-all-space` | conferma stessi 4 hunk |
| Hash blob confrontati | confermati |
| Verifica encodifica frecce | 41 = 41, equivalenza |
| Verifica semantica renderHomeWorkContext | corpo identico |

## Dichiarazione

Il runtime non è stato modificato. Nessun file tracciato è stato alterato. Nessun file non tracciato è stato committato. La pubblicazione non è stata attivata. Il push non è stato eseguito.
