# CML-160 — STORIA_CONTROLLED_RUNTIME_MAPPA_DATI_INTEGRATION

Data: 2026-06-26

## Stato iniziale

- Root Git: `C:/Users/anton/CurManLight`
- Branch: `main`
- Commit iniziale: `90163cf`
- `origin/main`: `90163cf`
- Working tree iniziale: pulito dopo `CML-160-PREFLIGHT-CLEANUP`
- Push: non eseguito
- Deploy: non eseguito

## Perimetro

Slice runtime controllata per integrare la mappa disciplinare di Storia nel runtime pubblicato locale.

File autorizzati:

- `_published_snapshot/netlify-current/index.html`
- `docs/03_execution/CML-160.md`
- `report/CML-160_storia_runtime_mappa_dati_integration.md`
- `docs/REPO-MOVELOG.md`

Non sono stati modificati schema `.cml`, import/export, service worker, workflow, validatori, dipendenze, root `index.html`, landing UI/UX o contenuti normalizzati JSON.

## Modifiche eseguite

In `_published_snapshot/netlify-current/index.html`:

- aggiunto il pulsante `Storia` nel selettore mappa disciplinare;
- aggiunta la variabile runtime `STORIA_MAPPA_DATI`;
- aggiunto il ramo di selezione in `renderMappaDisciplinare()`.

La struttura `STORIA_MAPPA_DATI` contiene:

- 5 strutture sostanziali;
- 5 nodi disciplinari;
- 9 progressioni verticali;
- 5 decisioni curricolari, tutte con stato `da_validare`.

## Fonti e prudenza curricolare

La mappa è derivata dal curriculum normalizzato Storia e dalla bozza CML-157C. Le decisioni relative a periodizzazione, raccordo Primaria/Secondaria, storia locale, memoria e cittadinanza restano esplicitamente `da_validare`.

## Controlli

Controlli eseguiti a chiusura:

- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS, 7 file / 94 unità / `overallValid: true` / 0 errori
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS, 7 discipline / 0 failed
- verifica Node del blocco `STORIA_MAPPA_DATI`: PASS, JSON parseable, S=5 N=5 P=9 D=5, decisioni `da_validare`, nessun `�`, `???` o `??`
- verifica assenza `.planning/codebase/`
- verifica assenza `jcode/`
- verifica assenza `57df9d8`

## Esito

La slice integra Storia nel runtime locale senza estendere altre discipline e senza modifiche collaterali fuori perimetro.

Verdetto atteso:

`CML_160_STORIA_CONTROLLED_RUNTIME_MAPPA_DATI_READY`
