# CML-078: Visual Affinity, Iconography, and Dark Mode Runtime Increment

## Goal
Implement light visual affinity refinements + automatic dark mode via CSS variables in the runtime HTML, following CML-077B option C selection.

## Files Modified
- `_published_snapshot/netlify-current/index.html` — CSS variables + dark mode block
- `docs/REPO-MOVELOG.md` — entry added

## Files Created
- `docs/03_execution/CML-078.md` — this document
- `report/CML-078_visual_affinity_iconography_dark_mode_runtime_increment.md`

## Changes

### 1. CSS Variables (`:root`)
Added design tokens for brand palette, spacing, border-radius, typography, and shadows:
- `--cml-primary`, `--cml-accent`, `--cml-bg`, `--cml-surface`, `--cml-card`, `--cml-text`, `--cml-border`
- `--cml-space-{xs,sm,md,lg}`, `--cml-radius-{sm,radius,lg,xl}`
- `--cml-shadow-{sm,md}`, `--cml-header`

### 2. Dark Mode (`@media prefers-color-scheme: dark`)
Comprehensive dark mode block (~350 lines) overriding all visual surfaces:
- Body: `#121212`, surfaces: `#1e1e1e`, cards: `#252525`
- All `.card` variants, `.toolbar`, `.aside-box`, `.norm-*`, `.guida-*`, `.esport-*`
- All `.tecnologia-*`, `.didattica-*`, `.curricolo-*` components
- Lists, filters, badges, tags, headers, footers
- Mobile bottom bar and menu overlay

### 3. Header Gradient
Replaced hardcoded `linear-gradient(...)` with `var(--cml-header)`

### 4. Iconography Normalization
Audited all icons across desktop tabs, mobile bottom bar, mobile menu, toolbar, and sub-nav:
- Tabs: 🏠 Home, 📚 Curriculum, 🧑‍🏫 Didattica, 📤 Esportazioni, ❔ Guida
- Bottom bar: 🏠 Home, 📚 Curr., 🧑‍🏫 Did., 📤 Esp., ☰ Menu
- Sub-nav icons: 📖 ✏️ 📋 📜 (Curriculum), 📊 📋 (Didattica)
- Result: all icons are Unicode/emoji, locally rendered, consistently paired with text labels

## No Changes
- No toggle manuale dark mode, no localStorage for theme
- No Tailwind, Google Fonts, remote icons/images, external libraries
- No backend, login, OAuth, API, database, cloud, or real authentication
- No schema `.cml`, export/import, role-access, or storage logic modifications
- `sw.js`, `_headers`, manifest, icons, PDFs, DESIGN.md — unchanged

## Verification
- ✅ Git status shows only `index.html` modified
- ✅ No CDN URLs added (pre-existing docx CDN is expected)
- ✅ No `localStorage`/`sessionStorage` modifications introduced
- ✅ No new functions or flow changes
- ✅ Icons remain Unicode/emoji, paired with text labels
- ✅ Dark mode only via `@media (prefers-color-scheme: dark)` — no JS toggle

## Next Step
CML-078A — live deploy smoke (15/15 PASS)
