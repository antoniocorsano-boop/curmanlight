# CML-154 - TECNOLOGIA_LIVE_RUNTIME_PARSE_ERROR_MICROFIX

**Stato:** completato.
**Verdetto:** `CML_154_TECNOLOGIA_LIVE_RUNTIME_PARSE_ERROR_MICROFIX_READY`

## Stato iniziale

| Parametro | Valore |
|-----------|--------|
| Branch | `main` |
| Commit iniziale | `da0c0e3` |
| origin/main iniziale | `eb6729f` |
| Stato iniziale | `main...origin/main [ahead 1]` |

CML-153 era ancora locale e non pushato. Nessun push eseguito prima del fix.

## Root cause

Errore browser riprodotto da CML-153:

```text
SyntaxError: Unexpected identifier 'esito'
```

Controllo sintattico inline script prima del fix:

```text
inline-script-1.js:3075
'<div class="curricolo-dept-validation-subtitle">Registra l'esito del controllo dipartimentale...
                                                            ^^^^^
```

La causa era una stringa HTML costruita in JavaScript e delimitata da apici singoli. Il testo utente conteneva `l'esito`, che interrompeva la stringa e bloccava il parsing dell'intero script runtime.

## Microfix applicato

File runtime modificato:

```text
_published_snapshot/netlify-current/index.html
```

Cambio minimo:

```text
Registra l'esito del controllo dipartimentale...
Registra l’esito del controllo dipartimentale...
```

Tipo microfix: apostrofo tipografico nel testo utente. Nessun cambio di significato, UI, layout, logica di rendering o service worker/cache.

## Controlli locali

| Controllo | Esito |
|-----------|-------|
| Pattern rotto `Registra l'esito` nel contesto JS | assente |
| Pattern corretto `Registra l’esito` | presente |
| Inline script syntax via `vm.Script` | PASS (`0 ok`, `1 ok`) |
| `TECNOLOGIA_MAPPA_DATI = TECNOLOGIA_MAPPA_DATI_GENERATA || TECNOLOGIA_MAPPA_DATI_FALLBACK` | presente |
| Ricerca altri pattern analoghi con apostrofo non escapato | nessun match |
| Validatore normalized curriculum | 7 file / 94 unita' / `overallValid: true` / `invalidCount: 0` |
| Harness runtime mappa dati | 7 discipline PASS, 0 failed |
| `git diff --check` | PASS |

## Smoke locale browser

Smoke Playwright su file locale corretto:

| Area | Esito |
|------|-------|
| Page errors | assenti |
| Console warn/error | assenti |
| `window.setMappaDisciplina` | `function` |
| `window.renderMappaDisciplinare` | `function` |
| `window.TECNOLOGIA_MAPPA_DATI` | `object` |
| Tecnologia | contenuti reali visibili nel contenitore mappa |
| Matematica | contenuti reali visibili nel contenitore mappa |
| Italiano | contenuti reali visibili nel contenitore mappa |
| Empty state visibile | no |
| Token tecnici visibili (`GENERATA`, `FALLBACK`, `TECNOLOGIA_MAPPA_DATI`, `var`, `debug`) | no |

## Vincoli

- Generator/adapter/transformer/validator invariati.
- JSON e schema `.cml` invariati.
- Nessuna estensione ad altre discipline.
- Nessun refactoring.
- Nessuna modifica service worker/cache.
- `.planning/codebase/` non reintrodotta.
- `jcode/` non usato.
- `57df9d8` assente dalle verifiche.

## Push e live

Da completare dopo commit: push controllato della sequenza CML-153 + CML-154 e smoke live `?v=cml154`.
