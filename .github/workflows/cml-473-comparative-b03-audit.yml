name: CML-473 Comparative B03 Audit

on:
  push:
    branches:
      - 'codex/cml-473-comparative-remote-b03-audit'
    paths:
      - 'content/gap/**'
      - 'schemas/gap-layer-v1.schema.json'
      - 'curman-react/**'
      - '.github/workflows/cml-473-comparative-b03-audit.yml'
  pull_request:
    branches: [main]
    paths:
      - 'content/gap/**'
      - 'schemas/gap-layer-v1.schema.json'
      - 'curman-react/**'
      - '.github/workflows/cml-473-comparative-b03-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cml-473-b03-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: curman-react
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: curman-react/package-lock.json
      - run: npm ci
      - run: npm run validate:gap
      - run: npm run test:b03
      - run: npm run test:cml472
      - run: npm run lint
      - run: npm run build
      - name: Install Playwright runner and Chromium
        run: |
          npm install --no-save --no-package-lock @playwright/test@1.54.1
          npx playwright install --with-deps chromium
      - name: Start Vite preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > cml-473-vite.log 2>&1 &
          echo $! > cml-473-vite.pid
      - name: Wait for preview
        run: |
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4173 >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat cml-473-vite.log
          exit 1
      - name: Run comparative Chromium audit
        run: npm run test:cml473
      - name: Upload audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cml-473-comparative-b03-audit
          path: |
            curman-react/playwright-report-cml473
            curman-react/test-results
            curman-react/cml-473-vite.log
          if-no-files-found: warn
