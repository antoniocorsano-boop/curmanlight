# CML-239.1 DEPLOY PAGES CONGRUENCE AND SYNTAX ERROR FIX

## Branch

- `main`

## Commit creati durante il lavoro

- Prerequisito docs-only CML-239: `0857038` (`docs: audit CML-239 pre-existing console errors`)
- Commit CML-239.1: `4e206c4` (`runtime: fix CML deployed syntax error`)

## Stato Git finale (atteso prima commit CML-239.1)

- `main...origin/main [ahead 2]`
- Working tree pulito

## File modificati CML-239.1

- `_published_snapshot/netlify-current/index.html`
- `_published_snapshot/netlify-current/sw.js`
- `docs/03_execution/CML-239.1.md`
- `report/CML-239.1_deploy_pages_congruence_and_syntax_error_fix.md`
- `docs/REPO-MOVELOG.md`

## Causa incongruenza live/locale

- Il deploy GitHub Pages pubblica correttamente `_published_snapshot/netlify-current`.
- Confronto blob/server: `index.html` congruente (`826952` byte, blob/hash allineati).
- La divergenza osservata in browser è dovuta a cache service worker con strategia `cache-first` anche per HTML di navigazione.

## Causa `Unexpected identifier 'open'`

- Non riscontrata nel file pubblicabile corrente (sintassi script OK).
- Errore riconducibile a copia stale lato client servita dalla cache service worker, con script non pienamente congruente allo stato pubblicato.

## Causa `setTab is not defined`

- Effetto a cascata della mancata inizializzazione completa dello script (parse error precedente).
- `setTab` è presente nel file pubblicabile e nei fetch congruenti.

## Fix applicato

1. `sw.js`
- bump cache version (`curmanlight-cache-v454-cml2391`)
- `network-first` per navigazione/HTML
- fallback offline su cache

2. `index.html`
- register SW versionato: `./sw.js?v=20260701-cml2391`
- `updateViaCache: "none"`

## Scope escluso confermato

- `docx.umd.min.js 404` fuori scope (CML-239.2)
- `favicon.ico 404` fuori scope (CML-239.2)

## Conferme vincoli

- Nessuna modifica curriculum JSON: confermato
- Nessuna modifica `tools`: confermato
- Nessuna modifica formato `.cml`: confermato
- Nessuna modifica schema storage: confermato

## Controlli

- `git diff --check`: PASS
- `node tools/validate-cml-normalized-curriculum.mjs`: PASS
- `node tools/test-runtime-mappa-dati-shape.mjs`: PASS (`14 passed, 0 failed`)
- Sintassi script file pubblicabile: PASS (`INLINE_SYNTAX_PASS`)
- Presenza `setTab`: PASS (`SETTAB_DEF_PRESENT`)
- Presenza sezioni Home/Curriculum/Guida/Processo/Esportazioni/UDA smart: PASS (`SECTIONS_PRESENT`)

## Smoke locale

- Verifica statica navigazione tab: PASS (`setTab('home'|'curricolo'|'guida'|'processo'|'esportazioni')` presenti)
- Verifica statica UDA smart / Programmazione: PASS (`didattica-step-uda-smart` presente)
- Nessun `SyntaxError` nel file pubblicabile rilevato da `node --check` sullo script inline estratto

## Rischi residui

- Client con service worker molto vecchio possono richiedere un ciclo refresh/update prima di allinearsi completamente.
- 404 docx/favicon restano noti e tracciati per CML-239.2.

## Prossima slice consigliata

- CML-239.2: gestione `docx.umd.min.js` 404 e `favicon.ico` 404 con fix separato.

## Verdict finale

`CML_239_1_DEPLOY_PAGES_CONGRUENCE_AND_SYNTAX_ERROR_FIX_READY_LOCAL_NOT_PUSHED`
